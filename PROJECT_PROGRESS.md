# Collector.Village 專案進度文件

> 這份文件用來追蹤整個專案的決策與進度，可以拿給其他 AI 對照討論，也方便自己回顧。
> 更新原則：每次跟 Claude 討論出新結論，就回來更新對應區塊。

---

## 專案總覽

**專案名稱**：Collector.Village
**核心概念**：收藏管理網站，玩家可以勾選自己擁有的動畫周邊商品，一開始以《進擊的巨人》為主軸，未來可擴充其他 IP（含 K-pop）。
**開發者背景**：一人開發，C 語言經驗 6 年，無遊戲開發/繪畫經驗，計畫用 AI 輔助寫程式。
**開發策略**：MVP 上線前的規劃階段，曾採「先用一般對話型 AI 把架構/內容/規格想清楚定案，最後集中請 Claude Code 一次性生成」的做法，目的是壓縮 Claude Code 訂閱月數。**此限制已於正式訂閱 Claude Code 後解除（2026-07-30 起）**：規劃討論與實際開發不再需要分開兩階段進行，可以隨時直接動工；規格仍然值得先想清楚再做，但不是為了省訂閱費，純粹是工程習慣。

---

## 目前狀態：MVP 核心功能已上線（2026-07-28）

**目前網址**：https://collector-village-nine.vercel.app/ 已經是正式版 Next.js + Supabase 網站（不再是靜態佔位頁），`/browse` 可瀏覽 400 件已匯入的商品資料。`nine` 是 Vercel 自動加的字尾，之後可在 Vercel 專案設定改名。
**本機專案路徑**：`C:\Users\Peggy\Downloads\Collecter.village.code`（Next.js + Supabase，已於 2026-07-28 commit 進 git）

> 詳細的「已完成頁面清單」與「下一步待辦」見文件最下方新增的「## 🖥️ Claude Code 開發進度」區塊，每次跟 Claude Code 討論完新進度都回來更新那一節即可，不需要重看整份規劃文件。

---

## ✅ 已定案項目

### 1. MVP 範圍界定
架構圖裡的城鎮、工廠、超市、算命館、社交系統等等，全部列為「未來擴充／可能不做」，**不在 MVP 範圍內**。
MVP 只做：收藏庫瀏覽 + 勾選擁有 + 篩選 + 收藏進度顯示。

### 2. 版權風險處理策略
- 資料庫**只存文字資訊**（商品名、系列、年份、出版社），不存官方圖片。
- 圖片只允許**玩家自行上傳**自己拍的實體照片，這是使用者生成內容（UGC），法律定位跟直接重製官方圖不同，風險大幅降低。
- 這個決策同時解決了原架構圖裡「侵權」「儲存空間」「商品圖授權」三個卡住的問題。
- **補充確認（2026-07-30）：掃描海報等平面美術品，等同直接複製官方美術，風險比照官網圖片，不能當作「像自己拍照」的低風險替代方案。** 只有拍攝自己實際擁有的立體實物（公仔本體等）才算低風險的使用者自產內容。此原則已反映在上傳照片介面的提醒文字與授權勾選（見第17項）。

### 3. 資料庫欄位設計（已建置上線）
表格：`ips`、`series`、`products`、`users`、`user_collections`、`collection_albums`、`album_pages`、`album_slots`、`product_submissions`
（完整、最新的 SQL 定義與 RLS 政策請見 `supabase/schema.sql`，該檔案可重複執行、隨欄位變動持續更新，此文件不再重複貼一份容易過時的複本；欄位設計背後的討論脈絡仍記錄在本節與下方各項）

商品分類（大分類 → 細分類，依粉絲角度整理，可再調整）：
- 立體公仔類：公仔、娃娃、立牌、抬頭娃、壓克力透卡
- 配戴隨身類：鑰匙圈、吊飾、髮夾、徽章、手機支架
- 包袋類：收納包、收納袋、環保袋、錢包
- 服裝類：衣服、娃娃衣服、外套、風衣
- 生活雜貨類：馬克杯、保溫瓶、紙杯墊、塑膠杯墊、桌墊、雨傘
- 紙製/影像類：海報、掛畫、資料夾、貼紙、色紙、拍立得、相卡、膠捲

版權補充原則：`image_url`、`source_url` **僅供管理者後台使用**，不對一般玩家顯示，用途是方便日後查核資料正確性（例如粉絲回報某商品年份標錯，可以快速連過去核對）或當作手繪參考，不會出現在任何公開頁面，維持跟前面版權策略一致。

一番賞系列命名參考範例（`series.name` 實際填寫格式）：
- 進擊的巨人～各自的決意～
- 進擊的巨人～獻出心臟吧～
- 進擊的巨人～地鳴～

### 4. 技術路線
- 不用遊戲引擎，走「Web 應用」路線：前端框架（建議 Next.js）+ PostgreSQL（用 Supabase 代管，內建登入系統跟圖片儲存）。
- 免費起步：Vercel/Netlify（網站託管）+ Supabase（資料庫+圖片儲存），都先用免費方案，等使用者變多、免費額度不夠再考慮花錢升級或買網域。

### 5. 角色標籤規則（雙層設計）
- **`characters`（主要角色標籤，陣列）**：上架時由自己判斷，商品畫面上出現的每個角色都填入，不因該角色戲份少而省略或簡化成「綜合」。理由：像莎夏這類劇情後期退場、周邊本來就稀少的角色，粉絲更需要靠精準標籤找到每一件商品；反之像里維、艾爾文這種周邊量本來就大的角色，即使某件商品沒標到他們，粉絲也還有很多其他選擇可買，影響較小。這層標籤用於篩選 UI。
- **`character_aliases`（角色別名/暱稱標籤，陣列，選填）**：給角色不同時期/身分的稱呼用，例如「漢吉」「團長漢吉」「戰損漢吉」。此欄位不影響主篩選清單，只輔助搜尋精準度。第一版由自己填寫基本標籤，之後開放粉絲協力補充別名也沒問題。
- 一個商品畫面上有多位角色時（例如徽章、資料夾常見組合：里維+艾爾文+漢吉；艾連+米卡莎+阿爾敏；約翰+莎夏+柯尼），比照上述原則全部列入 `characters`，不用「綜合」帶過。

### 6. 價格格式規則
- 統一存成顯示用字串（TEXT），不拆成數字+幣別兩欄（除非未來要做「依價格排序/篩選」或「總市值統計」功能，才需要拆開存）。
- 日幣：`¥780`；台幣：`NT$250`；非賣品：直接存文字 `非賣品`（不留空，避免跟「資料尚未填寫」混淆）。
- 一律不寫「円」「元」等單位字。

### 7. source_url 現況
- 先前用 Python 爬蟲產生的 source_url 皆未人工核對，判定不可信，先清空。之後查證商品資料時，逐筆手動填入該商品的實際頁面連結（不可用網站首頁代替）。

### 8. 分類擴充（category_group）
- 目前已知六大分類（立體公仔類、配戴隨身類、包袋類、服裝類、生活雜貨類、紙製/影像類）之外，確認新增：
  - **影音類**：DVD、藍光、CD、OST
  - **出版品類**：漫畫、畫集、設定集、雜誌特輯
  - **布娃娃類**（第24次討論新增）：玩偶、絨毛娃娃等（例如もちころりん系列），實測資料中發現此類商品量不少，正式納入第9個大分類
- 此欄位非寫死的列舉值，允許之後查證更多年份/商品時持續新增分類（例如電玩類是否要獨立於影音類，視之後查到的數量再決定）。

### 9. 市價相關需求 —— 不做獨立功能，沿用備註欄位即可
- 原本構想的「市價觀察日誌」（price_watch 表 + enable_price_tracking 開關）**決定不做**，改用 `user_collections.note`（既有的泛用備註欄位）取代。
- 理由：獨立功能等於在 schema／功能設計上明確表態「這是給買賣判斷用的」，容易觸及收藏圈對囤貨轉賣/黃牛行為的敏感觀感；泛用備註欄位不預設用途，玩家想記錄價格趨勢、想寫別的東西都可以，設計者不對任何行為背書。
- 技術上也更簡單：不需要新增資料表、不需要開關設定，玩家自行決定備註格式（例如自己手動寫「2026/01 ¥2200 → 2026/06 成交¥3200」）。
- **已定案**：`owned_status` 新增 `'wanted'` 狀態值，讓尚未擁有、僅觀察/想要的商品也能建立一筆 `user_collections` 紀錄，備註欄位才有地方可寫。

### 10. 虛擬擁有機制討論 —— GK 等高單價商品的「夢幻逸品」設計
- **起因**：GK 等高單價商品是「有錢有閒才玩得起」的收藏品類，一般粉絲買不起、也沒有實體空間收納/展示。使用者聯想到能否讓這類商品在遊戲化層面被「虛擬擁有」——例如透過遊戲內小遊戲/超市賺遊戲幣「買」到虛擬版本，布置在自己的虛擬小屋（構想類似動物森友會玩法）。
- **考量的正反意見**：
  - 支持理由：滿足買不起 GK 的粉絲的收藏慾望與情感投射，玩家自己清楚知道那是虛擬的、不會跟真實擁有混淆，屬於情感需求的延伸而非造假；跟收藏管理的核心精神不衝突。
  - 疑慮：專案先前已明確把「城鎮、工廠、超市、算命館、社交系統」列為「未來擴充／可能不做」（見 MVP 範圍界定），若現在整套遊戲經濟系統（賺遊戲幣、逛超市、蓋房子裝飾）都要做，會推翻「先壓縮規劃、最後一次性交給 Claude Code 生成」的開發策略，範圍大幅增加。
- **最終決定（拆成兩層評估）**：
  1. **虛擬擁有的「狀態標記」本身：現在就低成本納入。** `owned_status` 除了 `'wanted'`，再拆分出 `'owned_real'`（實體真的擁有）與 `'owned_virtual'`（虛擬擁有／夢幻逸品）。介面上先只用篩選標籤或小徽章呈現「虛擬擁有」，不做小屋展示畫面。
  2. **「如何賺到虛擬擁有資格」的遊戲機制（超市/小遊戲/賺遊戲幣/裝飾小屋）：維持列在 MVP 之外**，等核心收藏功能站穩、有使用者基礎後再評估要不要做成動物森友會式玩法。
  - 拆解理由：現在就能記錄「這是我的夢幻逸品，我用遊戲化的方式擁有它」這個情感需求，但不用在 MVP 階段承接一整套遊戲經濟系統的開發成本。
- **後續補充（第21次討論）**：「小屋擺放給其他玩家參觀」本質上是社交/公開展示功能，等同於已排除在 MVP 外的社交系統，因此小屋展示與其牽動的 UGC 內容審核問題（見第 21 項）自然一併延後，不影響現在的 MVP 開發排序，但需在啟動此功能開發前先落實審核機制。

### 10-1. 未來遊戲經濟系統的測試策略（第29次討論）
- **背景**：Peggy 設想之後測試工廠生產、商城買賣、遊戲幣等機制時，會需要模擬各種狀態（例如「餘額不足時是否正確跳出警告」「生產倒數計時是否正確完成」），一開始聯想到是否需要做一套後台系統或「開發者模式」帳號。核心疑慮：如果自己的帳號可以隨意改參數（餘額、計時），就沒辦法真實模擬一般玩家會遇到的情況。
- **結論：不需要後台系統，也不需要在 app 裡做「開發者模式」帳號／`is_admin` 開關。「作弊能力」完全不放進任何帳號或任何網頁，只存在於本機開發者自己執行的腳本裡，正式玩家帳號無論怎麼操作都碰不到這些能力。** 依測試情境分三種做法：
  1. **純邏輯判斷**（例如「餘額<成本要跳警告」）：寫自動化測試直接呼叫商業邏輯函式並斷言結果，不需要開瀏覽器手動測，也不需要任何測試帳號。
  2. **想親眼看畫面**（例如餘額不足時 UI 實際顯示什麼）：沿用專案既有的「本機腳本＋service role 金鑰直接寫資料庫」模式（比照 `scripts/backfill-product-codes.mjs` 的做法），寫類似 `scripts/dev-set-balance.mjs <user_id> <金額>` 的小工具，**只在本機開發環境的資料庫執行**，把測試帳號的資料庫欄位直接改成想要的狀態，再用瀏覽器登入該帳號實際操作查看。
  3. **想跳過等待時間**（例如生產倒數1小時）：兩種做法擇一，皆不需要在 app 裡寫任何特殊分支——(a) 用腳本直接改資料庫裡的時間戳記（例如把「開始生產時間」往前調），系統的「時間到了」判斷邏輯照樣走真實路徑；(b) 把倒數秒數設計成讀取環境變數，本機開發環境設短、正式站環境設定為真實時長，兩邊執行同一套程式碼、僅設定值不同。
- **理由**：這個做法讓測試帳號跟真實玩家帳號在部署上線的程式碼層面完全相同、不需要任何權限判斷邏輯，「改資料」的能力只存在於你自己終端機執行的腳本，不會意外出現在任何正式玩家能觸及的路徑上，同時完整保留「觀察真實玩家會遇到什麼」的測試價值。
- **範圍提醒**：此為未來遊戲經濟系統（工廠/商城/遊戲幣）的測試方法論，該系統本身仍屬於第10項已排除在 MVP 外的範疇，此處僅先定案「之後要測試時怎麼測」，不代表現在要開發。

### 11. 網站頁面結構規劃
共 6 個頁面，分兩區：**公開瀏覽區**（不需登入）、**會員專屬區**（需登入），中間以登入/註冊銜接。

**公開瀏覽區：**
- **首頁**：訪客入口，放專案簡介、精選商品、註冊/登入按鈕。MVP 階段可由現有佔位頁改造。
- **收藏庫瀏覽頁**：核心頁面，篩選條件對應 schema：IP、系列（series）、`characters`（角色主標籤）、`category_group`/`category`（大小分類）、`kuji_prize_tier`（賞別）。不需登入即可瀏覽，登入後每張商品卡片會多顯示個人的擁有狀態標記。
- **商品詳情頁**：單一商品完整資訊。登入後可在此切換三種狀態（`owned_real`/`owned_virtual`/`wanted`）、上傳實體照片、寫備註、填入手日期。

**會員專屬區（需登入）：**
- **小鎮總覽（第26次討論新增，登入後首頁）**：主題化的導覽 hub，把既有頁面包裝成「小鎮」的視覺概念。分兩類建築：
  - **已開放建築**（對應既有功能，無新開發成本）：**圖書館**＝我的收藏總覽；**家**＝收藏庫瀏覽＋想要清單。
  - **未來擴充建築**（僅在地圖上顯示、暫不可進入，呼應第10/21項已延後的遊戲經濟系統）：工廠、超市、市集、服飾店。這些建築先做成「看得到、點不進去」的視覺元素，成本低但能立即提供沉浸感與未來路線圖的期待感，底層邏輯不超出 MVP 範圍。
- **我的收藏總覽**（＝小鎮裡的「圖書館」）：個人儀表板，顯示整體收藏統計，並可依系列或角色分別統計。此頁需要的「收藏進度計算邏輯」尚待下一步定案。
- **想要清單**（歸於小鎮裡的「家」）：即收藏庫瀏覽頁篩選出 `owned_status = 'wanted'` 的視圖，不需另外做一套介面邏輯。
- **個人設定**：帳號基本管理（密碼、大頭貼），未來若有功能開關需求可往這裡擴充。

**關於資料夾/路由層級變動的說明**：頁面路由結構屬於低成本、可隨時調整的部分（不像資料庫 schema 涉及既有資料遷移），現在的頁面分層不是最終定案、之後隨時可依需要重新組織，不會有歷史包袱問題。

### 11-1. 美術風格決策流程（分階段處理，非現在定案）
- **背景**：使用者提出「小鎮」視覺概念後，發現無法想像 Claude Code 建置完成後的實際樣貌，也不確定該用哪種美術風格（純平面／2.5D／類3D／像素風等）。
- **關鍵釐清**：Claude Code 能處理的是網頁排版、互動邏輯、CSS 樣式，**不能生成建築插畫等美術素材**——這是兩種不同性質的工作，文字描述無法讓 AI 產生特定風格的插畫。
- **決定的處理方式（分三階段，難度由低到高）**：
  1. MVP 階段先用**純平面、簡潔的圖示風格**，不追求立體/3D質感，可用 AI 圖像生成工具產生風格一致的建築小圖示，不需要專業美術資源。
  2. 中期可視預算尋找接案插畫者，提供 3-5 張風格參考圖（例如玩家提到的摩爾莊園截圖），委託繪製一套建築/圖示。
  3. 若要做到 2.5D 或類3D質感，需要專門的美術資源（等角建築素材、角色動畫），建議待有明確預算/資源後再評估，不適合一人開發者在前期投入。
- **決定不在此時定案最終美術風格**：先用選項1完成可運作的網站雛形，有實際畫面後再評估美術方向是否需要調整，比現在憑空討論風格更有效率。

### 12. 收藏進度計算邏輯規則
- **核心原則：實體與虛擬分開計算，不合併顯示成同一個百分比。** `owned_virtual`（夢幻逸品）不計入核心「實體收藏進度」的分子，避免玩家靠虛擬擁有灌水出失真的收藏度數字。
- **依集合性質分兩種呈現方式，不可混用同一套邏輯**（此為本專案的核心倫理決策，AI與開發者日後都應遵守）：
  - **封閉集合（如單一一番賞系列，分母是官方公告時就固定不變的歷史事實）**：維持百分比呈現，例如「7/10 = 70%」。這種分母穩定可查證，百分比對玩家有真實意義（清楚知道還缺哪一項）。
  - **開放集合（如「某角色的所有商品」「全站總量」，分母是資料庫持續擴充中的估計值，永遠不代表官方實際總產出）**：**禁止用百分比呈現**，改用原始數字，例如「已收藏 312 件（資料庫目前收錄約 2000 件相關商品，持續擴充中）」，並附註「此數字反映目前已知範圍，不代表官方實際總產出」。理由：分母浮動時，百分比會製造「進度倒退」的錯覺（例如熱心玩家補充資料後，分母從2000變2500，原本50%的玩家瞬間變42%，但玩家實際擁有的東西完全沒變少），對開放集合而言百分比是失真且會誤導人的呈現方式。
- **統計範圍分三層**：
  1. 全站總進度（開放集合，用原始數字）
  2. 依系列統計（`series_id`，封閉集合，用百分比）
  3. 依角色統計（`characters` 陣列，開放集合，用原始數字）
- **多角色商品計算規則**：一件商品若 `characters` 陣列包含多個角色（例如里維+艾爾文+漢吉），此商品會完整計入每個角色各自的分母，擁有時也各自的分子 +1，不拆分比例。
- **`wanted` 狀態不計入任何進度分子**，只計入分母，純粹作為玩家個人追蹤清單使用。

### 12-1. 反焦慮／反衝動消費設計原則（重要，AI與開發者皆須遵守）
- **起因**：擔心百分比／收藏數字設計，可能誘發類似「遊戲成就解鎖」的心態，讓部分玩家（尤其未成年或衝動型消費者）為了湊齊進度而做出負擔不起的消費，或引發「誰收藏得多才是真粉絲」的比較心態與排他感（類比：拿演唱會場次數比較粉絲資格）。
- **已決定的防護原則**：
  1. **絕不做「集滿即可兌換／解鎖」機制**——這是典型會誘發過度消費的 gacha 設計，本專案明確排除，日後也不得追加。
  2. **不主動推播「只差一件就完成」之類的催促提醒**——玩家可以主動查詢自己的進度，但系統不得主動製造急迫感。
  3. **不做進度排行榜或玩家間的收藏數比較功能**（呼應 MVP 範圍已排除的社交系統，此處明確訂為原則而非僅是隱含排除）。
  4. **我的收藏總覽頁面用詞**：採用偏中性的「收藏紀錄」而非強調競賽/成就感的「進度」，弱化遊戲化催促感，強化「這是我自己的個人記錄」的語氣。

### 13. 使用者流程圖
主流程五步：**瀏覽收藏庫 → 商品詳情頁 → 標記擁有狀態 → 填寫備註/照片 → 我的收藏總覽**。
- 若使用者在商品詳情頁想標記狀態時尚未登入，導向登入/註冊頁，完成後直接返回原本瀏覽的那件商品繼續操作（避免登入完要重新搜尋一次的挫折感，實作上通常靠登入頁記住 redirect 網址達成，提醒之後跟 Claude Code 溝通時特別點出這點）。
- 「填寫備註/照片」為可選填步驟，勾選擁有狀態即可完成主要動作，備註跟照片是加分項，不強制填寫。

### 14. UI 文字/介面用語整理

**狀態標籤（對應 owned_status）：**
| 資料庫值 | 顯示文字 | 說明 |
|---|---|---|
| `owned_real` | 已擁有 | 綠色系標籤 |
| `owned_virtual` | 虛擬收藏 | 紫色系標籤，與「已擁有」明顯區隔 |
| `wanted` | 想要 | 灰色/外框樣式 |

**主要操作按鈕：**
- 商品詳情頁未標記：標記為已擁有 / 加入想要清單 / 標記為虛擬收藏
- 商品詳情頁已標記：編輯備註 / 上傳照片 / 移除標記
- 收藏庫瀏覽頁：篩選 / 清除篩選 / 依系列瀏覽 / 依角色瀏覽
- 我的收藏總覽：查看詳細進度 / 切換顯示虛擬收藏

**篩選面板標籤（對應 schema 欄位）：**
- 作品（IP，目前僅進擊的巨人一個選項）
- 系列/活動（對應 `series.name`）
- 角色（對應 `characters`，複選）
- 分類（拆兩層：先選大分類 `category_group`，再選細分類 `category`）
- 賞別（對應 `kuji_prize_tier`，僅一番賞商品出現）

**收藏進度顯示文字：**呼應第 12 項規則，「實體收藏進度」為主要顯示，「含虛擬收藏」為次要/可摺疊顯示，兩者不合併成單一數字。

### 15. 專案資料夾結構規劃
走 Next.js（全端框架），非傳統前後端分離的兩個專案，改以「頁面/元件/共用邏輯」三層分工：

```
collector-village/
├── app/                       # 頁面路由，對應6個定案頁面
│   ├── page.tsx               # 首頁
│   ├── login/
│   ├── register/
│   ├── browse/                # 收藏庫瀏覽頁
│   ├── products/[id]/         # 商品詳情頁（動態路由）
│   ├── dashboard/             # 我的收藏總覽
│   ├── wishlist/              # 想要清單
│   └── settings/              # 個人設定
├── components/                # 共用UI元件（商品卡片、篩選面板、狀態標籤等）
├── lib/
│   ├── supabase/              # Supabase client 初始化、資料庫查詢函式
│   └── utils/                 # 共用邏輯，例如收藏進度計算函式（對應第12項規則）
├── types/                      # TypeScript 型別定義，對應資料庫 schema 欄位
├── public/                     # 靜態資源（LOGO、預設圖，不含商品官方圖）
├── scripts/                    # CSV批次匯入工具（後台用，非玩家可見）
├── .env.local                  # Supabase 連線金鑰等環境變數
├── package.json
└── next.config.js
```

- `products/[id]/` 為動態路由，一個資料夾對應所有商品詳情頁，不需逐一開檔案。
- `scripts/` 專放後台/開發者工具，與玩家可見的 `app/` 頁面明確分開。
- `lib/utils/` 存放收藏進度計算等共用邏輯，避免散落在各頁面元件中，方便日後統一調整規則。

### 16. 收藏隱私範圍
- **預設：完全私人。** 登入使用者只看得到自己的 `user_collections` 資料（狀態、備註、照片），其他使用者的收藏內容一律看不到。
- 理由：直接對應第 12-1 項反焦慮設計原則。若收藏資料預設可被他人看到，等於留了後門讓「比較心態」（誰收藏得多才是真粉絲）透過非正式管道發生，與避免比較/焦慮的方向矛盾。
- **例外（列為 MVP 之後擴充，非現在要做）**：使用者可自行選擇產生「分享連結」，傳給朋友看自己的收藏。此連結為唯讀、不出現在任何公開列表、不可被搜尋、不顯示其他使用者的排名或比較——性質上是使用者主動、一次性的分享行為，跟系統驅動的排行榜/比較功能（已在12-1排除）完全不同，兩者不衝突。
- **此項「預設完全私人」需現在定案**，因為這是 Supabase Row Level Security（資料列層級權限）規則的基礎，若晚點才決定，會導致已寫好的權限邏輯需整批修改。

### 17. 照片上傳功能規格
- **儲存路徑結構**：`bucket: collection-photos`，路徑階層為 `user_id/product_id/檔名`（例如 `u123/p456/photo1.jpg`），天然對應第16項隱私規則——只要 bucket 權限設定為「僅該路徑對應的 user_id 本人可存取」，照片隱私就自動與收藏資料隱私一致，不需另外寫邏輯。
- **格式與大小限制**：接受 jpg/png/webp，單檔上限 5MB。**上傳前需在前端先壓縮/縮圖**（例如長邊壓至 1600px 內）——這是讓 Supabase 免費方案 1GB 儲存額度撐久一點的關鍵，不是可有可無的優化，避免玩家直接上傳手機原圖（動輒10MB+）迅速吃光額度。
- **一件收藏一張照片**：MVP 階段維持 `photo_url` 為單一 TEXT 欄位，不做多張照片（如正面/背面）。等實際觀察到使用者有此需求，再評估改為 `photo_urls TEXT[]`，現在不為未驗證的需求增加資料結構複雜度。
- **存取權限**：bucket 設為 private，前端讀取用 Supabase 簽名網址（signed URL）短期授權存取，非公開網址直接可看，避免使用者上傳的實體照片被猜網址存取。
- **上傳提醒與授權勾選（2026-07-30 定案並已實作）**：呼應第2項版權原則（掃描/描圖不算低風險替代方案），上傳照片區塊需以顯著文字提醒「僅限上傳自己實際拍攝的實體商品照片，不可使用官方圖片、海報掃描、電繪／描圖等平面美術重製方式」，並需勾選「我確認這是我自己實體拍攝的照片，並同意本站得於服務範圍內使用、顯示此照片」才能選取檔案上傳，未勾選時檔案輸入框停用。已實作於 `app/products/[id]/CollectionControls.tsx`。

### 18. CSV 批次匯入工具規格
- **決定不做「後台管理介面」，改用本機腳本**：目前規劃裡只有開發者本人會匯入商品資料，不需要多位管理者，若做一套「後台登入+上傳CSV+網頁結果介面」等於多蓋一整套管理者權限系統，對一人開發者是不必要的工程量，直接省下這塊，有助於壓縮 Claude Code 訂閱月數的開發策略。
- **做法**：放在 `scripts/` 資料夾（對應第15項資料夾結構）的本機 Node.js 腳本，開發者在自己電腦執行，用 Supabase service role 金鑰（具完整權限、不受 RLS 限制）直接寫入資料庫，不透過網站介面、不需額外的管理者登入機制。
- **腳本驗證邏輯**：
  1. 必填欄位檢查（`name`、`category`、`category_group`、`ip_id`），缺一則該筆跳過並記錄原因，不因單筆資料不完整讓整批匯入失敗。
  2. 重複偵測：以「系列 + 商品名稱 + 賞別」三者組合判斷是否為重複商品（避免單純比對商品名稱容易誤判），重複時預設跳過並列入報告，由開發者自行決定是否手動覆蓋。
  3. 執行後輸出結果報告（成功筆數、跳過筆數及原因、失敗筆數及原因），方便匯入後快速人工檢查。

### 19. 使用者註冊/登入功能規格
- **MVP 階段只做 Email + 密碼**，不做 Google/Facebook 等第三方登入（OAuth）。理由：OAuth 需額外設定第三方應用程式與授權流程，對 MVP 是增加複雜度換取「登入更方便」的錦上添花體驗，非核心功能；待網站有實際使用者、觀察到註冊步驟造成明顯流失時再評估加入。
- **登入只用 Email，不支援 username 登入**：`username` 欄位保留，但純粹作為玩家自選的顯示暱稱（例如未來若做分享連結功能，顯示用 username 而非 email），登入驗證只認 email，可直接使用 Supabase Auth 內建 email+password 機制，不需額外寫「username 查 email 再登入」的轉換邏輯。
- **Email 驗證信維持 Supabase 預設開啟**：註冊後需點驗證連結才能登入，雖對轉換率略有影響，但能有效擋掉垃圾帳號，對一人開發者而言值得這個取捨。
- **忘記密碼流程使用 Supabase 內建「重設密碼」信件機制**，不需自行開發。
- **社群連結（第21次討論補充）**：個人設定頁可選填 FB/IG/Threads 等純文字連結欄位（`users` 表可加 `social_links JSONB` 或類似欄位），僅供顯示、不涉及任何 OAuth 或第三方 API 串接。
- **分享到社群平台（第21次討論補充）**：不與 FB/IG 做 API 層級串接（需申請開發者權限、長期維護第三方 API 版本更新，對一人開發者是持續負擔）。改用瀏覽器內建的 **Web Share API**，讓使用者透過系統原生分享選單自行選擇分享目的地，維護成本趨近於零。

### 20. 官方商品回報流程（玩家協助回報遺漏項目）
- **對象**：一般玩家（非二創內容），用於回報資料庫中遺漏的正版商品。
- **流程**：介面設定頁提供「回報遺漏項目」按鈕，產生連結導向表單/頁面，讓玩家填寫跟 CSV 相同格式的商品資訊並提交。
- **審核機制**：提交後先寫入獨立的 `product_submissions` 表（欄位同 `products`，外加 `submitted_by`、`status`），**不需自建審核後台介面**，直接用 **Supabase 內建 Table Editor** 查看與核准，核准後用小腳本將資料搬移至正式 `products` 表。此設計避免多開發一套管理 UI，維持一人開發者的省力策略。
- **必須經過開發者本人審核後才會寫入正式資料庫**，避免玩家亂傳或重複/風格不一致的內容污染資料庫。

### 21. 二創 UGC 內容審核原則（未來擴充功能，先定原則）
- **背景**：使用者構想讓二創玩家透過遊戲內「工廠」自製內容並擺放在虛擬小屋展示給其他玩家參觀。此功能歸屬第10項已延後的遊戲經濟系統範疇，非 MVP 範圍，但涉及重大內容安全風險，先在此定案原則，待日後真正開發此功能時遵守。
- **核心判斷（重要，AI與開發者皆須遵守）**：**只要使用者上傳的內容可能被其他玩家看到，審核機制就不是可選項，而是法律責任問題**——不僅是一般成人內容的疑慮，更嚴重的是可能涉及未成年不當內容的風險，經營者即使不知情也可能需承擔法律責任。「公開展示」與「完全不審核」兩者不可並存。
- **已定案的審核架構（供日後實作參考）**：
  1. **上傳時強制先經自動化內容審核 API**（例如 Google Cloud Vision SafeSearch、AWS Rekognition 或同類服務），自動攔截明顯違規內容，被自動標記者才進入人工複查，而非人工逐一審查全部內容。
  2. **任何展示內容均需提供「舉報」按鈕**，任何使用者發現不當內容可一鍵舉報，開發者收到舉報後可立即下架，作為事後補救的最後防線。
  3. **註冊時的服務條款需明確列出禁止事項**（例如禁止上傳露骨/未成年不當內容），並保留開發者隨時停權帳號的權利。
  4. **保守替代方案**：若自動化審核的技術/成本門檻對一人開發者過高，UGC 內容可比照第22項官方商品回報流程，同樣先進入審核佇列由開發者本人核准後才公開展示——雖與「玩家自主生產不需審核」的初衷有落差，但在量能不足時是更安全的預設選項，待使用者規模擴大、有能力建置自動化審核後再開放即時公開。

### 22. 網站版權免責聲明
- **背景**：本站展示大量《進擊的巨人》等版權作品的周邊商品資訊，雖已透過「只存文字資訊、不存官方圖片、圖片僅玩家自行上傳」的策略降低重製風險（見第2項），但網站本身仍應有明確的免責聲明，這是獨立於資料庫設計之外、必須存在於網站頁面上的文字內容。
- **決定**：於網站頁尾（footer）固定顯示免責聲明，內容至少包含：本站為非官方粉絲自製之收藏管理工具，與原作、出版社、玩具製造商無任何關聯或授權關係；所有角色、商品名稱、商標均屬原權利人所有；商品資料僅供收藏管理參考，正確性以官方資訊為準。
- 此聲明文字待網站實際建置時，交由 Claude Code 或後續文案階段定稿，此處先確立「必須要有」與「應涵蓋的重點」。

### 23. 服務條款與使用者年齡政策
- **背景**：本站涉及使用者上傳照片內容（第17項）與未來的 UGC 功能（第21項），且第12-1項已明確關注避免誘導未成年人衝動消費，這些都需要一份正式的服務條款作為法律基礎，並且需要決定最低使用年齡。
- **台灣法規查證結果（2026/07/26 查證）**：台灣《民法》自2023年1月1日起將成年年齡從20歲下修為18歲；7歲以上未滿18歲屬「限制行為能力人」，其法律行為（如簽署契約）須經法定代理人承認才生效。台灣個資法本身**沒有**像美國COPPA那樣針對兒童訂出明確的年齡門檻，一般網路服務常見的「13歲」門檻是國際業界慣例，非台灣法定要求。本站不涉及金流交易，風險層級低於直接金錢交易類服務，但仍有曝險。
- **決定**：
  1. 註冊時需勾選同意服務條款（Terms of Service）與隱私權政策，未勾選不可完成註冊。
  2. **最低使用年齡定為13歲**（業界慣例），並於服務條款中明確加註「未滿18歲之使用者，需徵得法定代理人（監護人）同意方可使用本服務」，呼應民法限制行為能力人規定。
  3. 服務條款需明確列出禁止行為（例如上傳不當內容、騷擾其他使用者等），並保留開發者停權帳號的權利，呼應第21項審核原則。
- **重要提醒（非正式法律意見）**：上述為工程規劃層級的判斷與公開資料查證，非正式法律意見。待照片上傳、UGC功能等實際上線前，建議諮詢律師確認服務條款文字，尤其台灣另有《兒童及少年福利與權益保障法》等規範可能涉及刑事責任層級，超出本文件查證範圍。
- 條款完整文字內容非本文件範圍，待你決定要自行撰寫或請律師/範本協助時再處理，此處先定案「需要有」與「需涵蓋的要點」。

### 24. 帳號與資料刪除權利
- **背景**：使用者應有權利刪除自己的帳號與相關資料（常見的隱私法規要求，例如台灣個人資料保護法、歐盟GDPR等皆有類似規範），這點目前規劃中尚未提及。
- **決定**：個人設定頁（第11項頁面結構）新增「刪除帳號」功能，執行後需刪除或去識別化該使用者的 `user_collections`、上傳照片（Storage 中對應路徑）、`product_submissions` 中的提交紀錄等所有關聯個人資料，並且此操作需要二次確認（例如輸入密碼或打字確認），避免誤觸。

### 25. 網域命名與 SEO 策略
- **網域**：MVP 階段維持免費 Vercel 子網域，不急著購買正式網域。若之後想要的網域名稱被註冊走，屆時換一個名字即可，不影響開發排程。
- **SEO（搜尋引擎索引）**：MVP 階段商品詳情頁設為 `noindex`（meta tag 告知搜尋引擎不索引），只讓首頁與收藏庫瀏覽頁入口被索引。
  - 理由：商品詳情頁會大量出現角色名稱與商品名稱組合，若被 Google 完整索引等同主動提高在搜尋結果的曝光度，雖然資料庫本身已用文字資訊策略降低版權風險（第2項），但曝光度越高越可能被版權方注意到。
  - 現階段目標是服務核心粉絲小圈子，不靠 SEO 衝流量，不索引不影響現階段成長策略。
  - 「先保守、之後再開放索引」比「先開放、之後想收回」風險小很多（Google 收錄速度遠快於撤下速度），確定沒有版權疑慮後可再開放。

### 26. 商品資料處理標準流程（三階段）
- **背景**：實測發現不管用哪個對話型 AI（Gemini Pro、Meta AI等）爬取商品資料，格式可以套用規則、但內容正確性（尤其角色標籤判斷、系列名稱翻譯）仍需人工校對，這是資料本質問題（需要判斷圖片內容/理解規則），非特定 AI 能力優劣問題。
- **標準流程**：
  1. **AI 草稿**：不限工具，任一 AI（Gemini/Meta AI/Claude對話）依本文件規則爬取/整理商品資料為 xlsx/CSV 草稿。
  2. **自動驗證腳本**（待 Claude Code 建置）：檢查草稿是否違反規則，例如：`characters` 欄位是否出現「綜合」字樣、`category_group` 是否用了清單外的值、`series_name` 是否殘留日文原文未轉換、必填欄位是否缺漏、`official_price` 格式是否正確（`¥`/`NT$`/`非賣品`）。此腳本產出「違規清單」供人工快速定位問題，而非逐筆人工肉眼校對。
  3. **人工校對**：開發者依驗證腳本抓出的問題清單，針對性修正，不需要每筆從頭看起。
- 此流程可套用於任何後續新增的商品資料批次，不限單一 AI 工具或單一資料來源。

### 27. 佔位網站已上線
- GitHub repo：`collector-village`（存放靜態 index.html）
- 部署平台：Vercel 免費方案，串接上述 GitHub repo，自動部署
- 目前網址：https://collector-village-nine.vercel.app/
- 內容：純裝飾用佔位頁（假的收藏進度格子 + 訂閱通知表單，表單尚未串接真實後端）
- 名稱說明：「collector.village」是專案暱稱（把 collector_village 的底線換成點的寫法），不是真的網域後綴，不涉及網域搶註問題

### 28. AI 工具分工策略
- 一般聊天型 AI（Claude 對話、或其他）：想法討論、架構規劃、內容整理、文案。
- Claude Code（月費訂閱制，$20/月 Pro 方案即含）：最後階段實際建置專案、寫 API、串資料庫。
- 目前不用 Gemini 做正式資料整理（可靠度疑慮），改為由 Claude 對話 + 使用者自行查證真實商品資料。

### 29. 我的收納冊功能定案（第27次討論）
- **背景**：使用者構想一個把「已擁有」商品用類似實體小卡收納冊的視覺方式呈現的功能（畫面參考：收納櫃種類/名稱下拉選單、格狀商品排列、新增收納冊、左右翻頁），提出手繪 wireframe 討論。
- **與既有規劃的關係釐清**：
  - 這不是取代 `/browse`（收藏庫瀏覽頁，篩選清單式），而是新增一個獨立頁面，讓使用者用「收藏冊」的方式整理、瀏覽自己已擁有的商品。
  - 曾考慮做「小屋保險箱（私人）／書櫃（公開，訪客可逛進來翻看）」的空間視覺化分享方式，但這本質上等於重新做出第10/21項已明確排除在 MVP 外的「虛擬小屋訪問系統」，且「訪客隨意逛」的呈現方式容易誘發第12-1項要避免的比較心態。**最終決定不做**，改用下方簡化版分享機制。
- **分享機制（簡化版，沿用第16項「分享連結」設計）**：
  - 每本收納冊有「公開／私人」開關，預設私人。
  - 設為公開時，系統產生一組唯讀分享連結（不可搜尋、不出現在任何公開列表／探索頁），使用者自行決定要不要把連結傳出去，性質上是一次性、主動的分享行為，跟系統驅動的公開展示／排行榜不同。
  - **明確排除**：不做「瀏覽所有使用者公開收納冊」的探索/廣場頁面；不做小屋空間視覺化訪問系統。這兩點之後若使用者規模明顯成長，可再重新評估。
  - 公開檢視頁只顯示商品圖片與名稱，**不顯示備註、入手日期等個人資訊**。
- **商品放入規則**：只能從自己標記為 `owned_real` 或 `owned_virtual` 的商品中選取放入格子；`wanted` 狀態商品不可放入（尚未實際擁有，無實體卡可收納）。同一件商品可同時放入多本不同收納冊，但同一本收納冊內不可重複放入同一件商品。
- **版面格數規則（套用現實小卡冊固定規格，不做自由拖拉排版）**：
  - 1格（1×1）
  - 2格（1×2 或 2×1，使用者可選方向）
  - 3格（1×3 或 3×1，使用者可選方向）
  - 4格（固定 2×2）
  - 6格（固定 2×3）
  - 8格（固定 2×4）
  - 9格（固定 3×3）
  - 新增頁面時從上述模板擇一套用，不提供任意行列數的自訂排版，降低開發複雜度。
- **資料庫新增三張表**（`collection_albums`、`album_pages`、`album_slots`，完整 SQL 見下方 SQL schema 區塊）。
- **範圍界定**：此功能目前僅完成規劃討論，尚未交給 Claude Code 開發，待整理成具體指令後再排入開發順序。

### 29-1. 我的收納冊：選商品放入格子的操作方式（2026-07-28 補充定案）
- **背景**：功能已依 `claude-code-instructions-albums.md` 開發完成後，使用者提供手繪 wireframe（`idea/收納冊初版.png`）作為原始發想參考，畫面呈現「商品清單常駐在旁邊、可拖拉進收納冊格子」的操作方式，與初版實作（點格子才彈出選單、選完關閉）不同。
- **討論結果**：
  1. **常駐商品清單這個方向採納**：商品清單改為固定顯示在編輯畫面旁邊（桌機為左側欄、手機因版面較窄改為堆疊在格子下方），不再是點格子才彈出、選完就收起來的彈窗形式，瀏覽/比對商品時不用重複開關選單。
  2. **不做真正的拖拉（drag and drop）**：手機/平板用觸控拖拉的操作體感通常不佳（沒有滑鼠 hover 的即時反饋、容易誤觸拖錯格子），而收藏整理這類操作預期會有不少使用者在手機上進行。改維持「先點格子選定目標，再點商品清單裡的項目放入」兩步驟點選流程，桌機與手機體驗一致。之後如果想額外支援滑鼠拖拉當進階捷徑，可以再疊加上去，不影響現有點選流程。
  3. **wireframe 裡其他呈現方式**（用下拉選單切換收納冊、左右兩頁攤開同時顯示、收納冊類型為固定分類按鈕而非自由文字）目前**不採納**，維持原本規格（獨立列表頁 `/albums`、一次顯示一頁＋上下頁切換、`album_type` 自由文字欄位），先以較低開發成本的版本上線，之後有需要再重新評估。

### 30. 玩家互助補圖功能定案（第28次討論，v1 規格）—— v1 已於2026-07-30開發完成並通過本機測試，尚未部署上線
- **背景**：起因是 Peggy 打算先自行上傳手邊實體公仔照片，讓早期玩家一進站就看到部分商品已有圖，避免網站看起來像空殼/詐騙站。討論延伸出「開放玩家協助補圖」的完整構想，此處收斂為 v1 定案版本。**2026-07-30 補充**：原本因應「壓縮 Claude Code 訂閱月數」策略把此類功能列為「規劃好、之後才動工」，但該限制已隨正式訂閱 Claude Code 解除（見專案總覽開發策略），因此 v1 已直接動工開發完成，細節見「Claude Code 開發進度」的對應小節。
- **商品分兩層圖**：
  1. **商品「公版代表照」**：全站瀏覽該商品時看到的圖，初始為空。
  2. **玩家個人收藏頁的照片**：沿用既有已定案機制（第17項），私有、僅本人看得到，不需審核，玩家自行對內容負責。
- **v1 核心簡化規則（關鍵）：只在該商品「完全沒有公版代表照」時，才開放玩家提交候選圖成為公版圖；一旦某商品已經有公版代表照（不論是 Peggy 自己上傳、或其他玩家提交審核通過的），就不再開放其他玩家提交「取代」該公版圖的候選圖——此時其他玩家只能透過第17項既有的個人照片機制更新「自己那端」看到的圖，不影響、也不能覆蓋全站公版圖。**
  - 理由：討論中發現若開放「更好的圖可以取代已核准公版圖」，會出現「自己的圖被換掉」的失落/比較心態（呼應第12-1項反焦慮設計原則，性質上等同變相的排行榜競爭）；同時能把 Peggy 的審核工作簡化成「這張圖能不能用」的二選一判斷，不需要持續評比多張候選圖優劣，也避免同一商品堆積大量候選圖佔用 Storage 空間。
  - 「取代已核准公版圖」機制**目前不做**，列為更之後才評估的擴充；若之後要做，需先想清楚如何避免比較心態（例如公版圖不掛名貢獻者）。
- **Peggy 身為站長兼玩家的身分**：Peggy 自己上傳的圖直接視為公版圖來源，不需要走待審流程——審核的目的是確認內容/版權/是否對應正確商品，Peggy 本人上傳的自己已確定，不需要另外審核自己。
- **審核機制**：比照第20項「官方商品回報流程」既有模式，玩家提交寫入獨立審核佇列表 `product_photo_submissions`（商品ID、提交者、圖片路徑、狀態），**不建管理後台介面**——實作上 Peggy 直接去 Supabase Storage 頁面瀏覽候選圖縮圖，決定後執行對應的本機腳本（approve/reject）完成核准/駁回與後續資料搬移，核准後才寫入商品的公版代表照欄位供全站顯示。
- **獎勵機制**：玩家提交的照片經審核通過、正式成為該商品公版代表照後，可獲得 **10 枚遊戲幣** 獎勵。此獎勵設計刻意不依賴「圖片被選為官方圖」的虛榮心／競爭誘因，即使沒有這個誘因，遊戲幣本身就是實質獎賞，也降低了玩家為搶當「官方圖」重複上傳競爭的動機（呼應上述 v1 簡化的理由）。
- **遊戲幣帳本雛形已建置**：呼應第10-1項「先做帳本、不做餘額」的設計，approve 腳本審核通過時會在新表 `game_currency_ledger` 寫入一筆 +10 紀錄。完整的遊戲幣系統（花費、兌換、餘額顯示等）仍屬於第10項已排除在 MVP 外的遊戲經濟系統範疇，尚未設計/建置，但至少不會漏算這段期間玩家應得的獎勵。
- **授權需求**：呼應第23項服務條款，不論提交公版圖候選或私人照片，上傳當下都需明確勾選同意「授權本站於服務範圍內公開展示此照片」，避免版權/授權爭議。

### 31. 工廠系統 v1 定案（第31次討論）
- **背景**：Peggy 發現「收藏庫」資料整理（人工校對 products.csv）非常花時間，且瓶頸在她本人（不是 Claude Code），於是決定兩邊並行：她繼續人工校對 Excel 的同時，讓 Claude Code 開發原本列在第10項「MVP之外」的遊戲經濟系統之一──工廠。與收藏庫是完全獨立的系統，互不干擾（工廠做出來的是虛擬二創周邊，不是官方商品）。
- **v1 範圍刻意縮小，只做以下項目，其餘列為 v2 之後再談**：
  1. **只做自動模式**：輸入固定材料一定產出固定數量成品，不做手動小遊戲（節奏/QTE 裁切玩法，見 `idea/節奏遊戲.png` 構想），因為手動模式規則還沒想清楚，先求有一版能玩、好玩再說。
  2. **原料從「原料商店」直接用遊戲幣購買**，不做「種植/收割」上游生產鏈（那是 v2 構想：例如先種棉花、收割後才有布料原料庫存），v1 也不做原料庫存倉庫——玩家選機器+格式+設計圖的當下就直接扣幣開始生產，不會有「先囤原料、之後再拿來生產」的中間狀態。決定不先為 v2 的原料庫存結構預先建表，因為 v2 細節（收割機制、隨機產量等）都還沒想清楚，先建等於用猜的，猜錯了照樣要重做；且產品能不能撐到開發 v2 也還不確定，過早佈局的成本效益不高。
  3. **不開放玩家上傳自己的圖（UGC）**：v1 的「設計圖庫」由 Peggy 自行到網路上尋找標示可商用/可修改（避免 NC 非商業限制授權）的圖片，玩家只能從這個圖庫選圖生產，玩家自己上傳原創圖案列為 v2 才做（那時才需要處理完整的授權切結、浮水印、原圖只有玩家自己看得到等機制，呼應第21項UGC審核原則的精神）。圖庫本身用不到的候選圖片先放在本機 `local-assets/factory-designs/`（已加入 `.gitignore`，比照現有「本機審核資料夾不進git」慣例），審核篩選後再用腳本上傳到新的 Supabase Storage bucket。
  4. **超市只做「系統固定價格收購/販售」**，不做玩家互相買賣。v2 待開放UGC上傳後才考慮加入玩家展示（例如超市裡放告示板讓玩家填自己的 IG/Threads/賣場連結），但即使那時候有「玩家買賣」，也只允許用遊戲幣交易虛擬展示品，不涉及真實金流；真的想要下單周邊本體，是透過告示板連結，讓買家自行到遊戲外（例如 IG 私訊）跟賣家聯繫，遊戲本身只提供「展示/導流」功能，不介入實際交易，避免詐騙/糾紛的客服負擔。
  5. **工廠做出來的成品用獨立的「工廠倉庫」頁面展示**，不併入現有「我的收藏」（那套是綁定官方真實商品的 `products`/`user_collections`，混在一起會讓「真實擁有」跟「虛擬二創周邊」的概念混淆）。`/town` 小鎮頁面既有的「工廠」建築（原本是灰階佔位、點不進去）將改為可進入。
- **機器／原料／格式對應表**（每台機器對應一種原料大類，同一台機器可做該原料底下所有格式，選哪個格式只影響產出數量與售價，跟選哪張設計圖無關；圖庫可以之後隨時擴充不影響經濟平衡）：

| 機器 | 原料(1份成本) | 可選格式 | 每份原料產出 | NPC收購單價 | 整批賣掉總收入 | 淨賺 |
|---|---|---|---|---|---|---|
| 印表機 | 紙材 30幣 | 海報 | 4張 | 25幣 | 100幣 | +70 |
| | | 明信片 | 10張 | 10幣 | 100幣 | +70 |
| | | 小卡 | 20張 | 5幣 | 100幣 | +70 |
| | | 貼紙 | 25張 | 4幣 | 100幣 | +70 |
| 裁縫機 | 布料 50幣 | 娃娃 | 4隻 | 40幣 | 160幣 | +110 |
| | | 娃衣 | 2件 | 80幣 | 160幣 | +110 |
| 壓模機 | 鐵料 60幣 | 徽章 | 12個 | 16幣 | 192幣 | +132 |
| | | 鑰匙圈 | 8個 | 24幣 | 192幣 | +132 |
| 雷雕機 | 壓克力板 80幣 | 壓克力立牌 | 2個 | 125幣 | 250幣 | +170 |
| | | 壓克力吊飾 | 10個 | 25幣 | 250幣 | +170 |

  - 各格式投報率抓在 212~233% 左右，刻意讓玩家選格式是看喜好而非看賺錢效率；壓克力因為單價貴、產出少，符合「比較費工」的直覺。
  - 二創圈目前實際最常賣、熱度最高的品項其實是壓克力立牌/吊飾（比徽章更指標性），因此第一版就決定把雷雕機/壓克力這條線一起做，不等v2。
  - 生產等待時間定為 **10~30分鐘**（比真正工廠遊戲短很多），刻意求快，理由是希望玩家能常常打開遊戲玩，而不是掛著等好幾小時。
  - **四台機器可以同時獨立生產**，不會因為在用印表機就不能同時用壓模機——同樣是為了讓玩家常回來看的高頻互動感，若同時間只能用一台機器，大部分時間都在乾等，會降低這個目的。
- **遊戲幣帳本沿用現有設計，不用改 schema**：`game_currency_ledger.amount` 欄位當初就設計成可正可負（見第30項建表 comment「正數為獲得，負數為花費」），花錢買原料只要寫一筆負數紀錄，餘額 = 全部加總，第10-1項「先做帳本不做餘額」的決定對這次的花費需求依然適用，不需要另外加一個餘額欄位。
- **v2 待討論清單（本次明確列為之後才處理，不是現在要做）**：手動小遊戲生產模式的規則、玩家上傳原創UGC圖案（含授權/浮水印/審核機制）、原料改為「種植/收割」上游鏈並搭配原料庫存倉庫、超市開放玩家用遊戲幣互相買賣虛擬展示品＋外部連結告示板導流。

### 32. 超市系統 v1 定案（第32次討論，取代原本工廠倉庫的「全部賣掉」即時收購）

- **背景**：原本工廠倉庫頁面的「全部賣掉」按鈕，其實就是第31項「超市只做系統固定價格收購」規則的 v1 實作，只是暫放在工廠倉庫頁、沒有獨立的超市頁面。這次討論把它具體化成一個真正的「便利商店」式系統，`/town` 的「超市」建築也從灰階佔位改成可進入。
- **核心機制：貨架計時器，不是逐件計時**。玩家花幣買「貨架」，**整個貨架共用「每分鐘賣出 1 件」的速度**，不是每種商品各自計時——這樣計時器數量等於玩家買了幾個貨架，不會因為上架很多種商品就有大量計時器同時跑。想加快出貨速度，得買更多貨架平行賣，不是塞更多東西到同一個貨架。
- **貨架容量以「總件數」計算，不分商品種類（2026-08-01 修正）**：原本第一版誤設計成「最多放幾種商品」（例如固定2格），Peggy 指出應該是「總共能放幾件」，玩家可以自由分配要放幾種、各放幾件（例如容量10可以選4件A+6件B，或5件A+3件B+2件C），賣掉的件數會即時釋放空位，不用等整批賣完收款才空出來。上架介面也配合改版（見下方開發進度小節），從「每個空格一張表單」改成 `idea/上架系統.jpg` wireframe 那樣：列出工廠倉庫所有可上架品項，勾選一項＋輸入件數＋按上架。
- **同貨架多商品時的賣出順序：先進先賣完，不輪流**。跟工廠生產佇列用同一招：`active_from` 在上架當下就依「這個貨架目前排隊裡最晚的預計賣完時間」算好、之後不再變動，不需要背景排程，讀取時用時間差推算目前賣了幾件、還剩幾件（呼應第10-1項的既有做法）。
- **下架**：把還沒賣完的數量收回工廠倉庫，下架前會先結算「已賣掉但還沒收款」的部分入帳，不會讓玩家平白損失已經賣出的錢。
- **收款是明確動作，不是自動入帳（2026-08-01 補充定案：本日營業額）**：貨架每分鐘「賣出」只是數量上的推算，賣出的錢不會直接變成可花的遊戲幣，而是先累加顯示成「本日營業額」——這樣才有「在經營一間店」的感覺，不是東西一賣掉錢就自動到手。玩家可以隨時點「結算明細」純粹用同一批已經抓回來的資料在前端算給自己看（賣了什麼商品各幾件、各多少錢），不會動到資料庫；要真的把營業額變成遊戲幣，得按「入帳」，這個動作才會寫入 `game_currency_ledger`，入帳邏輯用「目前推算賣出數量 - 已收款數量」算差額，同一筆短時間按兩次入帳差額會是0，不會重複入帳。這個設計讓超市完全不需要背景排程/cron，跟系統其餘部分的技術路線一致。「入帳」是**帳號層級**的動作（一次結算名下所有貨架），不是每個貨架各自收款。
- **上架時不顯示成本，只選商品跟數量**：成本已經在工廠選格式生產的當下決定過一次，超市階段不重複顯示，也不開放玩家自訂售價（v1固定用成本的固定倍率，不做「拖拉調售價」那種機制）；玩家可以自由決定要上架庫存裡的哪一款設計圖、上架幾件（不用整批倉庫存貨一次上架完，可以拆著上架不同款式到不同貨架）。
- **超市有「營業中／暫停營業」開關（全站只有一個，不分貨架）**：暫停時所有貨架的倒數凍結、不會有東西被賣掉，但玩家仍可以照常上架/下架商品；重新營業時把暫停的時長整批補回所有貨架格子的排程時間，讓凍結效果真的延續到重新營業後（技術細節見下方開發進度小節），純粹是營造「像真的在開店」的氣氛，沒有機制上的特殊目的。
- **工廠倉庫容量上限（促成「工廠↔超市」資源循環的關鍵）**：倉庫預設容量 300 件（不分商品種類），花幣可升級、每次 +50。收成時如果會超過容量會直接擋下來，逼玩家要嘛去超市上架清空間、要嘛花錢升級倉庫，不能無限生產又不處理——這是這次討論確認「不想讓玩家無限產出」之後加的機制，讓工廠跟超市變成真正互相牽動的循環，而不是兩個獨立系統。
- **新手起始資源（解決雞生蛋問題）**：拿掉「全部賣掉」即時收購後，新玩家理論上會卡在「沒錢生產、也沒錢買貨架」——所以註冊當下直接發放起始遊戲幣（草案150枚）+ 1 個免費貨架，讓玩家能馬上開始玩，不用先想辦法生出第一筆錢。**這個起始贈禮只對新註冊帳號生效**（寫在 `handle_new_user` trigger 裡），既有帳號不會回溯拿到，測試帳號可以直接用既有餘額走真正的「買貨架」流程測試。
- **草案經濟數字（可調整，都集中在 `lib/market/catalog.ts` 一個檔案）**：買貨架 150 幣、貨架容量 10 件（2026-08-01 由「2種」改成「10件」）、倉庫升級 500 幣／次、每次 +50 容量、新手起始 150 幣+1貨架。這些數字都是先抓個開始測試用的草案，不是精算過的平衡數字，試玩覺得不對可以隨時調。
- **上架動作放在超市頁面，不放工廠頁面（2026-08-01 討論定案）**：Peggy 提議過乾脆在工廠頁生產結果旁邊直接加「上架到貨架X」的捷徑，Claude 建議放在超市頁比較合理——玩家需要看到各貨架的容量/現況才能決定要放哪個貨架，這個判斷只有在超市頁才有完整資訊；工廠頁再塞一套貨架選擇邏輯等於重複實作一次超市頁的內容。已定案維持在 `/market`，工廠倉庫頁只保留「去超市上架」的連結。
- **貨架依機器/類別分開（例如印表機的東西只能放貨架1、雷雕機的只能放貨架2，像便利商店飲料櫃跟冰淇淋櫃分開）——v1 不採用**：Peggy 提出這個構想但也不確定要不要做，Claude 的建議是先不加這個限制，理由是 v1 貨架本身還沒有視覺/家具差異（所有貨架長得一樣），現在加類別限制只會變成一個沒有視覺回饋的庫存管理麻煩，不會有「這個貨架看起來就是放飲料的」那種對應感；比較適合等第29-1項提到的空間佈置/家具系統（相框/書櫃/洞洞板）做出來之後，讓「這個家具的形狀本來就只放得下某類商品」這件事自然發生，而不是額外訂一條規則。v1 維持任何貨架可放任何商品。
- **v1 刻意不做的部分（留到之後，呼應美術/範圍分階段的既有原則）**：貨架的空間佈置（30×30可擴充網格、相框/書櫃/洞洞板等不同展示家具、牆壁vs地板擺放，含上方的機器類別分櫃構想）目前只做**列表 UI**，不做真的空間視覺化；等這套「賣貨機制」本身測過覺得好玩，再沿用「收納冊」已經驗證過的「點格子選目標→點商品放入，不做真拖拉」模式做視覺化空間版本（第29-1項），不重新設計一套新的互動方式。

### 32-1. 手動上架／自動上架模式（2026-08-02 補充）

- **背景**：手動上架（含「自動上架」按鈕一次補滿當下空位，見開發進度小節）還是需要玩家自己回來按，Peggy 想要一個「開了就不用再管，系統自己一直補到倉庫清空為止」的模式。
- **定案**：`/market` 頁「營業中／暫停營業」開關旁新增第二個開關，切換「手動上架」（預設，維持原本行為：玩家自己上架，賣完就停在那裡不會自動補）／「自動上架」（開啟後，系統自動把所有貨架補滿，一路補到工廠倉庫庫存全部上架完為止，不分商品種類、不用玩家自己動手）。
- **技術做法（沿用「不跑背景排程」既有原則）**：`lib/market/restock.ts` 的 `autoRestockUser()` 在「玩家造訪 `/market` 頁面」跟「導覽列每45秒輪詢通知摘要 `/api/notifications/summary`」這兩個既有的時間點順便觸發，不新增背景排程/cron；玩家完全沒開網頁的期間不會偷偷補貨，跟超市其餘「讀取時用時間差計算」的精神一致。補貨依貨架建立時間依序，每個貨架依工廠倉庫庫存清單順序由上到下補滿空位，直到所有貨架補滿或倉庫庫存用完為止；跟手動的「自動上架」按鈕用同一套排程鏈接規則（新上架的接在同貨架排隊最晚的 slot 之後）。
- **新增 `public.users.market_auto_restock` 欄位**（預設 `false`），新增 API `app/api/market/toggle-auto-restock/route.ts`（切換開關，開啟當下立刻跑一次補貨，不用等下一次輪詢）。
- `tsc`／`lint`／`build` 皆過，**尚未經真人瀏覽器測試**，等 Peggy 這輪一起測。
- **Peggy 實測抓到兩個問題，皆已修正（2026-08-02，同日第二輪）**：
  1. **工廠出貨口圖示（📦）改成看佇列，不看倉庫庫存**（見 `idea/圖示不對.png`）：原本判斷「工廠倉庫裡這台機器的成品庫存 > 0」，但倉庫庫存包含很久以前收成、還沒拿去超市上架的舊庫存，導致玩家看到生產序列明明是空的（0/4）卻出現貨堆圖示，跟畫面上看到的東西對不起來。改成只看「佇列裡有沒有已完成、還沒按收成的批次」（`components/factory/MachineCard.tsx` 的 `hasReadyBatch`），跟畫面上會不會出現「收成」按鈕完全一致，不再需要把 `inventory` 傳進 `MachineCard`。
  2. **冒煙／Zzz 圖示改成看整個佇列，不是只看排最前面那一批**：原本只判斷 `batches[0]`（ready_at 最早的一批），但這一批完成後如果玩家還沒按收成，會一直卡在佇列最前面，這時候真正在跑的其實是排在它後面、還在倒數的那一批，卻因為只看第一筆而誤判成 Zzz（見 `idea/圖示不對.png`：壓模機第一批已完成、第二批其實還在倒數0:34，卻顯示Zzz）。改成 `producing = batches.some(b => ready_at > now)`——只要佇列裡還有任何一批沒倒數完就算在生產中。
- **超市自動上架合併同款商品，不再一直新增列（2026-08-02，依 `idea/排序太長.png` 定案）**：自動上架模式每次補貨量通常很小（貨架每分鐘賣掉1件，就補1件回去），原本每次都新增一個 slot，同一款商品排隊中很快就變成一長串「同款商品 × 1」的重複列。新增共用邏輯 `lib/market/listing.ts` 的 `upsertShelfSlot()`：上架時检查貨架排在最後面的那個 slot（不論正在賣還是排隊中）是不是跟這次要上架的品項（同格式+同設計圖）一樣，是的話直接把數量累加上去、不新增列；不是的話才真的新增一列接在後面。因為只跟「最後一個」slot 合併，合併時只需要改它自己的 `quantity`（`active_from` 不用動，賣完時間自然因為數量變大而順延），後面沒有其他 slot 需要跟著調整，不需要額外的級聯搬移邏輯。`/api/market/list`（手動上架、也是 ShelfCard「自動上架」按鈕依序呼叫的同一支API）跟 `lib/market/restock.ts`（被動自動上架模式）都改用這個共用函式，確保上架的寫入邏輯只有一份。
- `tsc`／`lint`／`build` 皆過，**這輪修正尚未經真人瀏覽器測試**，等 Peggy 下一輪測試確認。
- **自動上架畫面不會自己更新的 bug（2026-08-02，同日第三輪，見 `idea/沒有補滿.png`）**：Peggy 開啟自動上架後盯著畫面看，一分鐘過去貨架沒有補滿，換頁或重整才看到補上。原因：補貨其實有在背景正確發生（導覽列每45秒輪詢通知摘要時就會觸發 `autoRestockUser`），但 `/market` 是伺服器元件，資料只在頁面載入當下抓一次，本身不會自動重新整理——資料庫其實已經悄悄補過貨，畫面卻停在舊的數字。新增 `components/market/MarketAutoRefresh.tsx`：自動上架模式開啟時，每 15 秒呼叫一次 `router.refresh()`，讓貨架空位／待上架清單／倉庫庫存持續跟資料庫同步；手動上架模式沒有背景寫入行為，不受影響，維持原本只在玩家操作後才重整。
- `tsc`／`lint`／`build` 皆過，**這輪修正尚未經真人瀏覽器測試**，等 Peggy 下一輪測試確認。
- **上一輪修正互相影響、產生新的競態 bug，已修正（2026-08-02，同日第四輪，見 `idea/系統還沒更新.png`）**：Peggy 測試時發現同一款商品（兔子小卡）被拆成一長串各自 1~2 件的重複列，而不是預期的「合併成一列、數字持續變大」。
  - **根因**：這是上面兩項修正**互相疊加**造成的新問題，不是「改一次bug就一定會壞一次」的通則，而是這兩個改動剛好都會影響「補貨多久觸發一次」——`MarketAutoRefresh` 讓 `/market` 頁面每15秒觸發一次補貨，而補貨邏輯本身（`app/api/notifications/summary` 輪詢時）在45秒的節點也會觸發一次，兩者時間點重疊時，會同時各自讀到「貨架最後一個slot還是別的商品」的舊狀態，各自誤判成「需要新增一列」，而不是等對方寫完再合併——`lib/market/listing.ts` 的 `upsertShelfSlot()` 判斷邏輯本身沒有鎖，兩個同時執行的請求看到的都是彼此寫入前的畫面。
  - **修法**：拿掉 `app/api/notifications/summary` 這條觸發途徑，只留 `/market` 頁面（初次載入 + 每15秒自動重整）這一條，減少同時執行的機會。
  - **殘餘風險（刻意接受，比照第31/32項對虛擬遊戲幣競態的既有判斷）**：這不是徹底解決競態問題，例如玩家同時開兩個 `/market` 分頁理論上還是可能重現；但後果純粹是畫面上多幾列（不影響總數量、不影響遊戲幣正確性），這些多出來的列會隨著各自賣完自然從畫面上消失，屬於低風險、可接受，不值得為此導入額外的鎖機制（例如 Postgres advisory lock）。**目前畫面上已經拆散的那些列不用手動清，會自己賣完消失，不影響已經入帳/未入帳的金額正確性。**
- **`MarketAutoRefresh` 改成精準對時，取代原本每15秒無腦重整（2026-08-02，同日第五輪，Peggy 提出的架構建議）**：Peggy 指出固定每15秒重整一次是照目前「每分鐘賣1件」這個數字硬猜出來的間隔，以後如果賣出速度改慢（例如5分鐘賣1件）還是固定15秒重整，大部分時間都在做白工，也完全沒有理由要「輪詢」——應該精準算出「下一次真的會有東西被賣掉」的確切時間點，只在那個時間點才重整一次。
  - **改法**：`MarketAutoRefresh` 改吃 `slots` 與 `marketOpen`，用 `slot.active_from` + 已經過幾個 `MARKET_SELL_MINUTES_PER_UNIT` 週期，算出所有貨架裡「正在賣的那個 slot」下一次會少一件的確切時間點，取最早的一個排一次性的 `setTimeout`（不是 `setInterval`）。重整拿到新資料後 effect 會依新的 `slots` 重新算下一次時間點，形成精準鏈式排程，不管賣出速度未來怎麼調整都不用改這個元件的程式碼；暫停營業時不會有任何東西被賣掉，直接不排程。
  - 這個改法附帶讓上一項的競態風險又更低一階：因為重整時機變成精準對準每次賣出事件（而不是每15秒的固定節奏），同時執行的機會比原本更小。
- **真正根因找到並修正：不是競態問題，是合併邏輯本身在「打平」時會認錯隊伍尾端（2026-08-02，同日第六輪，見 `idea/又出現.png`）**：Peggy 換用精準排程後仍然看到同款商品被拆成好幾列，直接連正式資料庫查詢比對後找到根因。**這一項是對上面兩輪「殘餘風險/競態」判斷的更正**：不是分頁併發競態造成的，而是 `upsertShelfSlot()` 判斷「貨架排在最後面的是哪個 slot」時，只看「哪個 slot 算出來的預計結束時間最晚」，遇到兩個**不同商品**剛好算出一模一樣的結束時間（同樣1分鐘賣1件、量體湊整的情況下不算罕見），比較邏輯只認「嚴格更晚」，打平時會沿用先在查詢結果裡出現的那一筆，認錯了真正的隊伍尾端；之後的補貨接在錯的分支後面，讓同一個貨架的排程整個分岔成兩條並行的隊伍，而不是一條。**這不只是畫面好看與否的問題**：一旦分岔，未來排到那個時間點時，理論上會出現兩樣不同商品同時被判定為「正在賣」，違反「先進先賣、一次只賣一樣」的核心設計（見已定案項目32），先前把這個現象講成「純畫面問題」不夠精確，這裡更正。
  - **修法（`lib/market/listing.ts`）**：(1) 找隊伍尾端時，先排除「已經完全賣光、只是還沒被玩家按收款清掉」的舊 slot，避免新庫存被合併進一個早就賣完的死格子而憑空消失；(2) 結束時間打平時，改成比較「哪一筆比較晚才被建立（`listed_at`）」來判斷真正的尾端，不再看查詢結果的出現順序；(3) 額外幫合併寫入加了樂觀鎖（`.eq('quantity', tail.quantity)`），如果讀取後、寫入前被別的請求搶先改過，安全地改成新增一列，不會覆蓋別人剛寫入的東西。
  - **既有測試資料已清空重來**：這個 bug 已經讓 Peggy 測試帳號的貨架排程分岔了一段時間，修好程式碼不會回頭修正舊資料，所以新增 `scripts/dev-reset-market-warehouse.mjs`（清空前會先把已賣出未收款的部分結算入帳，不會平白讓賺到的遊戲幣消失，接著才清空 `market_shelf_slots` 跟 `factory_inventory_items`，貨架本身、工廠生產佇列、遊戲幣帳本歷史都不受影響），已對 Peggy 帳號執行過一次（結算入帳6168幣、清空2個貨架、清空工廠倉庫）。
- `tsc`／`lint`／`build` 皆過，**這輪修正尚未經真人瀏覽器測試**，等 Peggy 下一輪測試確認。
- `tsc`／`lint`／`build` 皆過，**這輪修正尚未經真人瀏覽器測試**，等 Peggy 下一輪測試確認。
- `tsc`／`lint`／`build` 皆過，**這輪修正尚未經真人瀏覽器測試**，等 Peggy 下一輪測試確認。

---

## 討論紀錄摘要（時間序）

| 日期/次序 | 討論內容 | 結論 |
|---|---|---|
| 第1次 | 架構圖檢視 | 確認核心吸引力，界定 MVP 範圍，指出版權風險 |
| 第2次 | 資料庫設計 + AI分工 | 完成 schema 設計，決定用文字資料庫避開版權問題 |
| 第3次 | Gemini 初步任務 | 決定不採用 Gemini 做正式資料整理 |
| 第4次 | AI 訂閱選擇 | 決定買 Claude Pro（含 Claude Code），先不買 Max |
| 第5次 | 開發策略調整 | 決定先用對話完成所有非程式規劃，最後集中用 Claude Code |
| 第6次 | 佔位網站上線 | 用 GitHub + Vercel（皆免費）部署靜態佔位頁，澄清「collector.village」只是暱稱非網域，Supabase 留待後續串接資料時再用 |
| 第7次 | 商品分類系統 | 依粉絲角度分類清單，整理成大分類/細分類兩層，新增賞別、製造商、圖片連結、來源網址欄位，圖片連結確認只存外部連結不重製 |
| 第8次 | 欄位用途澄清 + 開始頁面結構規劃 | 確認 image_url/source_url 僅供後台管理者使用、不對玩家開放；分類分組方式獲確認；決定先做網站頁面結構規劃，CSV稍後上傳 |
| 第9次 | 上傳試爬 xlsx 商品資料 + 重訂細部規則 | 發現舊資料缺大分類欄位；角色標籤改為雙層設計（characters 主標籤全列出 + character_aliases 別名輔助）；價格格式統一為 `¥`/`NT$`/`非賣品` 字串；source_url 判定不可信全數清空；分類新增影音類、出版品類；市價相關需求決定不做獨立功能，改沿用泛用備註欄位（避免觸及收藏圈對黃牛行為的敏感觀感）；owned_status 新增 'wanted' 狀態值 |
| 第10次 | 虛擬擁有機制討論（GK夢幻逸品） | 考量買不起GK的粉絲情感需求 vs 專案先前排除的城鎮/超市/遊戲經濟系統範圍衝突，決定拆層處理：owned_status 再拆出 'owned_real'/'owned_virtual' 兩種狀態（低成本，現在納入），但賺遊戲幣/逛超市/裝飾小屋等遊戲機制維持排除在MVP之外 |
| 第11次 | 網站頁面結構規劃 | 定案6個頁面，分公開瀏覽區（首頁、收藏庫瀏覽頁、商品詳情頁）與會員專屬區（我的收藏總覽、想要清單、個人設定），想要清單視為收藏庫瀏覽頁的篩選視圖不需獨立介面邏輯；下一步待訂「我的收藏總覽」需要的收藏進度計算邏輯 |
| 第12次 | 收藏進度計算邏輯規則 | 定案實體/虛擬進度分開計算不合併顯示；三層統計範圍（全站/系列/角色）；多角色商品完整計入每個角色分母分子不拆比例；wanted 狀態只算分母不算分子 |
| 第13次 | 使用者流程圖 | 定案主流程五步：瀏覽收藏庫→商品詳情頁→標記擁有狀態→填寫備註/照片（可選填）→我的收藏總覽；未登入時導向登入/註冊後應返回原商品頁繼續操作 |
| 第14次 | UI 文字/介面用語整理 | 定案狀態標籤顯示文字、主要操作按鈕文案、篩選面板標籤命名、收藏進度顯示文字（呼應第12項實體/虛擬分開顯示原則） |
| 第15次 | 專案資料夾結構規劃 | 定案 Next.js 目錄樹（app/頁面路由、components/共用元件、lib/含supabase與進度計算utils、types/、scripts/後台匯入工具），規劃階段清單僅剩商品資料CSV整理待處理 |
| 第16次 | 進度百分比倫理疑慮討論 | 發現「開放集合分母浮動」會製造進度倒退錯覺（熱心玩家補資料後分母變大，其他人進度反而下降），修正規則：封閉集合（單一系列）用百分比，開放集合（角色/全站總量）改用原始數字不用百分比；新增12-1反焦慮設計原則：禁止集滿解鎖機制、禁止催促提醒、禁止排行榜比較功能、用詞改為「收藏紀錄」弱化競賽感 |
| 第17次 | 收藏隱私範圍 | 定案預設完全私人（Row Level Security 基礎規則），「分享唯讀連結」列為MVP之後可選擴充，與已排除的排行榜/比較功能區隔開（主動一次性分享 vs 系統驅動比較） |
| 第18次 | 照片上傳功能規格 | 定案儲存路徑結構（user_id/product_id階層）、格式限制jpg/png/webp單檔5MB、前端需先壓縮縮圖（節省Supabase免費額度）、MVP先做單張照片不做多張、bucket設為private搭配簽名網址存取 |
| 第19次 | CSV批次匯入工具規格 | 決定不做後台管理介面，改用本機Node.js腳本（用Supabase service role金鑰直接寫入，省下管理者權限系統開發量）；定案驗證邏輯：必填欄位檢查、系列+商品名稱+賞別組合判斷重複、執行後輸出結果報告 |
| 第20次 | 使用者註冊/登入功能規格 | 定案MVP只做Email+密碼、不做OAuth第三方登入；username僅作顯示暱稱不用於登入；開啟Supabase預設Email驗證信；忘記密碼沿用Supabase內建重設機制 |
| 第21次 | 上傳萬代一番賞正式格式xlsx；官方回報流程、UGC審核原則、社群分享方式；AI自主補充遺漏項目 | 確認xlsx格式正確套用新規則（待使用者後續複查內容正確性）；定案官方商品回報流程用Supabase Table Editor人工審核、不建管理後台；定案UGC內容審核原則（強制自動化審核+舉報機制+服務條款，因公開展示內容涉及法律責任非可選項）；小屋展示歸類為已延後之社交功能；社群分享改用Web Share API不做OAuth串接，個人設定可選填社群連結；AI主動補充三項先前未提及但重要的項目：網站版權免責聲明、服務條款與最低使用年齡政策（暫定13歲，待法規確認）、帳號與資料刪除權利 |
| 第22次 | 台灣最低使用年齡法規查證 | 查證台灣民法2023年起成年年齡改為18歲，7-18歲為限制行為能力人需法定代理人同意；台灣個資法無COPPA式明確兒童年齡門檻；正式定案最低使用年齡13歲（業界慣例）+服務條款加註未滿18歲需法定代理人同意；明確標註此為工程規劃判斷非正式法律意見，實際上線前建議諮詢律師 |
| 第23次 | 網域命名與SEO策略 | 定案MVP階段不買正式網域，維持免費子網域；商品詳情頁設為noindex避免SEO曝光提高版權風險，僅首頁與收藏庫瀏覽頁入口被索引 |
| 第24次 | 建置提示文件產出+資料處理流程定案 | 產出獨立的claude-code-build-brief.md收斂27項定案內容供交給Claude Code使用；定案商品資料處理標準流程（AI草稿→自動驗證腳本→人工校對三階段），不限單一AI工具 |
| 第25次 | Gemini爬蟲CSV驗證與清理 | 上傳460筆Gemini搜尋成果CSV，執行驗證腳本抓出：28筆重複表頭列、11筆重複商品、3筆欄位錯位、1筆美金報價、15筆系列名稱殘留日文；正式採納「布娃娃類」為第9個大分類；使用者決定不逐筆複查，直接移除全部19筆有問題資料，最終保留402筆乾淨資料，遺漏部分留待玩家回報機制（第20項）補足 |
| 第26次 | 小鎮總覽視覺概念+美術風格決策流程 | 新增「小鎮總覽」為登入後首頁，包裝既有頁面（圖書館=收藏總覽、家=收藏庫/想要清單），未來擴充建築（工廠/超市/市集/服飾店）以「看得到點不進去」方式先呈現，不超出MVP範圍；釐清資料夾/路由層級屬低成本可調整項目；定案美術風格分三階段處理（MVP先用平面圖示風格，不現在決定最終風格） |
| 第27次 | 「我的收納冊」功能討論（wireframe手繪圖） | 定位為新增獨立頁面，不取代`/browse`；曾考慮「小屋保險箱/書櫃」空間視覺化分享方式，因等同重做已排除的小屋訪問系統且易誘發比較心態而否決，改採沿用第16項分享連結機制的簡化版（每本收納冊可設公開/私人，公開產生唯讀連結，不做探索廣場頁）；格數採固定套用現實小卡冊規格（1/2/3/4/6/8/9格），不做自由拖拉排版；資料庫新增collection_albums/album_pages/album_slots三表；規劃完成，待整理成Claude Code開發指令 |
| 第28次 | 玩家互助補圖功能定案（v1） | 定案 v1 核心簡化規則：只在商品完全沒有公版代表照時開放玩家提交候選圖，已有公版圖後不開放取代，避免比較心態與審核負擔（呼應12-1）；審核比照第20項模式（Table Editor人工審核，不建UI）；站長自己上傳免審核；通過審核可獲10枚遊戲幣獎勵，但遊戲幣系統本身仍屬第10項已排除範疇，尚待建置；上傳需授權公開展示（呼應第23項） |
| 第29次 | 未來遊戲經濟系統的測試策略 | 討論工廠/商城/遊戲幣未來要怎麼測試（餘額不足警告、生產倒數計時等），確認不需要後台系統或app內的「開發者模式」帳號；改用本機腳本＋service role直接改資料庫狀態（純邏輯用自動化測試、想看畫面用腳本改本機DB、跳過等待用腳本改時間戳記或環境變數控制秒數），讓測試能力完全不出現在正式玩家能碰到的程式碼路徑上，同時保留觀察真實玩家體驗的測試價值 |
| 第30次 | 開發策略解除限制＋玩家互助補圖v1實際開發 | 澄清「壓縮Claude Code訂閱月數」的規劃/開發分離策略已隨正式訂閱解除，之後可隨時直接動工；趁 Peggy 校對 products.csv 期間，實際開發第30項「玩家互助補圖」v1：新增 official_photo_path 欄位、product_photo_submissions 與 game_currency_ledger 兩張表、兩個Storage bucket、商品頁補圖UI、三支審核用本機腳本，並用真實資料完整測試過提交/核准/駁回流程後清空測試資料，已通過build/lint/tsc，尚未push |
| 第31次 | 工廠系統 v1 定案 | Peggy 發現收藏庫人工校對Excel很花時間、瓶頸在她本人不在Claude Code，決定兩邊並行：她繼續校對、Claude Code 開發工廠系統；定案v1只做自動模式（不做手動小遊戲）、原料直接用遊戲幣跟原料商店買（不做種植/收割/原料庫存，v2再說）、不開放UGC上傳（v1圖庫由Peggy自行尋找可商用素材）、超市只做系統固定價格收購販售（不做玩家互相買賣）、成品用獨立「工廠倉庫」頁面展示不與官方收藏庫混合；定案四台機器（印表機/裁縫機/壓模機/雷雕機）對應四種原料與各自可選格式的完整換算表與投報率（212~233%）、生產等待10~30分鐘、四台機器可同時獨立生產；確認 game_currency_ledger 的 amount 欄位本來就支援負數花費，不需改schema |
| 第32次 | 超市系統 v1 定案＋收藏庫校驗暫緩 | Peggy 決定收藏庫 CSV 人工校驗先暫緩（工作量比預期大），改跟 Claude Code 討論並開發超市系統；帶了 AI 生成的工廠動畫參考影片（拆成 idea/印表機1~5.png），確認那個等級的3D美術 Claude Code 做不出來，定案美術分工：Peggy 用 Gemini Pro 生圖、Claude Code 寫 prompt+把素材串成動畫；先做不依賴美術的動畫骨架（MachineScene 五階段狀態機）；超市設計從「一鍵全賣光」升級成「貨架計時器」機制：貨架共用每分鐘賣1件的速度（不是逐件計時）、同貨架多商品先進先賣完不輪流、上架/下架、收款動作明確化不背景自動入帳；新增工廠倉庫容量上限（300起，可花幣升級+50）促成工廠↔超市資源循環；新增新手起始資源解決移除即時收購後的雞生蛋問題；確認開發策略沒選錯（網頁適合核心產品的URL分享/SEO需求，遊戲化邏輯也不需要即時渲染引擎，不用換Godot/Unity）；空間佈置視覺化（貨架/家具/網格）留到v1之後，屆時沿用收納冊已驗證的點選式互動模式 |

---

## 🖥️ Claude Code 開發進度（2026-07-28 更新）

> 這一節專門記錄「程式碼實際做到哪」，跟上面的「規劃討論紀錄」分開。每次請 Claude Code 做完新功能，回來更新這節就好，不用整份文件都重看。

### 已完成頁面/功能

| 路徑 | 說明 |
|---|---|
| `/`（首頁） | 專案簡介 + 精選商品 + 註冊/登入 CTA；文案於 2026-07-28 依使用者提供文字定稿（標題/副標、現在已可做的事、開發中預告、結尾號召） |
| `/login`、`/register`、`/forgot-password`、`/reset-password`、`/auth/callback` | Email+密碼登入註冊、忘記密碼、Supabase Auth 回呼 |
| `/browse` | 收藏庫瀏覽，含篩選面板（作品/系列/大小分類/賞別/角色）+ 分頁 |
| `/products/[id]` | 商品詳情頁：標記已擁有/虛擬收藏/想要、編輯備註+入手日期、上傳照片（前端自動壓縮長邊至1600px再傳 Supabase Storage，上傳前需勾選「實體拍攝＋授權使用」確認框才能選檔，2026-07-30 新增）；商品若有公版代表照則顯示於頂部，沒有的話顯示「幫忙補一張照片」提交表單（2026-07-30 新增，見第30項） |
| `/town` | 小鎮總覽（登入後首頁）：圖書館(=dashboard)、家(=browse/wishlist) 可進入，工廠/超市/市集/服飾店灰階佔位 |
| `/dashboard` | 我的收藏紀錄：全站(開放集合,原始數字)/依系列(封閉集合,%)/依角色(開放集合,原始數字) 三層統計，實體與虛擬分開顯示；依系列/依角色每一列可點擊展開，顯示實際貢獻該數字的商品清單並可點入商品詳情頁（2026-07-31 新增，見下方說明） |
| `/wishlist` | 想要清單，重用 browse 的 ProductCard，篩選 `owned_status='wanted'` |
| `/settings` | 暱稱、社群連結、改密碼、刪除帳號（需輸入確認字串，`/api/account/delete` 用 service role 清 Storage 照片後刪除 auth 使用者，DB 資料靠 FK cascade 自動清除） |
| Header 導覽列 | 全站共用，依登入狀態顯示不同連結 |
| `supabase/schema.sql` | 完整建表 SQL + RLS 政策 + `collection-photos` Storage bucket 設定，可重複執行 |
| `scripts/import-products.mjs` | CSV 批次匯入腳本（`npm run import:products`），必填欄位檢查+重複偵測(系列+名稱+賞別)+內容規則警告(綜合標籤/分類清單外/日文殘留/價格格式)，執行後印出成功/跳過/警告報告 |
| `/albums` | 我的收納冊清單：新增收納冊、刪除收納冊，卡片顯示頁數與公開/私人狀態 |
| `/albums/[id]` | 收納冊編輯：九選一版面模板新增頁面、上下頁切換、**常駐側邊商品清單**（點格子選定目標→點商品放入，不做拖拉，見已定案項目29-1）、格子右上角 × 直接移除、公開/私人切換（切公開自動產生 `share_token`）+ 複製分享連結 |
| Footer 版權免責聲明 | 全站共用（`components/Footer.tsx`），2026-07-28 依使用者提供文字定稿，小字灰階呈現 |
| `/albums/share/[token]` | 公開唯讀檢視，不需登入；只顯示商品名稱與使用者自己上傳的照片，不顯示備註/入手日期/擁有狀態；未公開或 token 不存在一律回 404，不透露私人收納冊存在 |
| `/factory` | 工廠 v1（見已定案項目31）：四台機台（印表機/裁縫機/壓模機/雷雕機）選格式+選圖生產，每台可同時排隊最多4批依序生產（2026-07-31 改版，不用每10~30分鐘就開一次遊戲）、收成進工廠倉庫（有容量上限，可花幣升級，見第32項）。設計圖庫可用真正圖片或純文字佔位。與官方收藏庫完全獨立的系統，不進 `products`/`user_collections`。**原本的「全部賣掉」即時收購已拿掉**，變現要去 `/market` |
| `/market` | 超市 v1（見已定案項目32，2026-08-01 新增）：買貨架、把工廠倉庫的東西上架到貨架空格，貨架每分鐘自動賣出1件（同貨架多商品先進先賣完，不輪流），上架/下架、收款（結算入帳）皆為明確動作。`/town` 的超市建築已從灰階佔位改成可進入 |

驗證狀態：`npm run build`、`npx tsc --noEmit`、`npm run lint` 均通過；dev server 逐一路由 curl 測試無誤（受保護頁面未登入時正確 307 導向 `/login`）；`products.csv` 已實際匯入資料庫，`/browse` 顯示 400 件商品共 9 頁，`/products/1` 等詳情頁確認能正確顯示真實資料。**正式環境（Vercel）已於 2026-07-28 部署成功並用 curl+瀏覽器截圖驗證首頁/browse/商品詳情頁皆正常顯示真實資料。**收藏標記/備註/照片上傳/我的收藏統計/想要清單/個人設定/刪除帳號整條流程也已用真實測試帳號在正式環境操作過一遍，全部正常（刪除帳號會連 Storage 照片、`auth.users`、`user_collections` 一併清乾淨，已用腳本查證）。

### 我的收藏紀錄：依系列/依角色展開明細（2026-07-31 新增）

- **背景**：Peggy 回報「依角色」加總看起來比實際收藏件數多、且系列/角色列點下去沒反應，懷疑是 bug。查證後確認數字計算沒有錯——是同一件標了多位角色的商品（呼應第12項多角色商品規則），會同時計進每個角色各自的分子，加總本來就會超過實際件數；但「點擊沒反應」是真的，因為原本那兩排就只是純顯示用的 `<li>`，從未實作互動。
- **新增**：新增 `components/dashboard/ExpandableProgressRow.tsx`（client component），讓「依系列」「依角色」每一列可點擊展開／收合，顯示實際貢獻該數字的商品名稱清單，並可點擊連到 `/products/[id]`；虛擬收藏的項目會標註「虛擬收藏」。純資訊呈現，不涉及排行榜/比較，不牴觸第12-1項反焦慮原則。
- **已驗證**：`tsc --noEmit`／`lint`／`build` 皆通過；本機 dev server 用真實測試帳號登入操作，確認點擊「里維·阿卡曼」與對應系列列都能正確展開出同樣兩件商品，點商品名稱能正確導到商品詳情頁。

### 工廠系統 v1（2026-07-31 開發完成，對應第31項）

- **新增資料庫物件**（`supabase/schema.sql`，已套用到正式資料庫）：`factory_designs`（設計圖庫，`is_active` 控制上下架）、`factory_production_batches`（生產批次，`quantity`/`material_cost` 用下單當下的快照值，不受之後調整經濟數字影響）、`factory_inventory_items`（工廠倉庫庫存，依「格式+設計圖」歸類）；新增 public bucket `factory-designs`。機器/原料/格式的完整換算表（成本、產出、售價、生產分鐘數）刻意不建表，寫死在 `lib/factory/catalog.ts`。
- **權限設計**：一般角色對三張新表都只有 SELECT（讀自己的資料／讀已上架設計圖），沒有 INSERT/UPDATE 權限；開始生產／收成／賣出這三個會動到遊戲幣或生產數量的操作，一律透過 `app/api/factory/{start,collect,sell}/route.ts` 用 service role 執行（比照 `/api/account/delete` 的既有模式），避免玩家繞過網頁直接竄改資料庫。
- **開發過程中發現並修正兩個真的會影響正確性的問題**（不是規格變更，是實作階段用真實資料測試時抓出來的 bug）：
  1. **時區位移導致生產「秒完成」**：`ready_at` 等欄位原本用 `timestamp`（不含時區），但應用層是用 `Date.now()` 算出的絕對時間存進去，資料庫連線階段的時區設定會讓存進去的值悄悄位移了 8 小時，導致倒數計時形同虛設、一生產就顯示「可以收成」。改成 `timestamptz` 後正確。
  2. **同一台機器可以被同時啟動兩批生產**：「檢查這台機器是否忙碌→沒有就寫入新批次」這兩步中間有空隙，兩個幾乎同時送出的請求會同時通過檢查、各自寫入一筆，實測真的重現了這個狀況。修法是在資料庫加一個 partial unique index（`(user_id, machine_key) where status='in_progress'`），由資料庫保證同一台機器同一時間只有一筆生產中，應用層原本的預先檢查保留當作「使用者體感較快的提示」，但真正擋下重複生產的是這個 DB constraint；同時把「先扣款再建立批次」的順序倒過來（先建立批次成功後才扣款），避免請求因為撞到這個 constraint 失敗時玩家的錢已經先扣了卻沒得到生產批次。
  - 提醒：目前**沒有**處理「同時啟動不同機器、剛好都通過餘額檢查導致餘額被花超支」這種更罕見的競態情況（例如同時按下印表機跟壓模機的開始生產，兩個請求都讀到同一份餘額）。這屬於刻意的簡化取捨：虛擬遊戲幣就算短暫出現負數，帳本本身還是準確、可追溯，沒有真實金錢或資料損毀風險，跟「同一台機器被連點兩次」這種很容易不小心觸發的情況比起來，發生機率低很多，先不處理。
  - **（2026-07-31 補充）此點的 partial unique index 已於下方「工廠排隊生產」改版時移除**，因為現在同一台機器本來就允許同時有多筆 `in_progress`（排隊），詳見下一節。
- **已驗證**：`tsc --noEmit`／`lint`／`build` 皆通過；本機 dev server 用真實測試帳號（先用腳本暫時發放測試遊戲幣，測試完已清空）跑過完整迴圈：選圖→開始生產→扣款正確→倒數計時正確（用腳本把 `ready_at` 撥回過去來快轉測試，比照第10-1項既有的測試方法論，不等真的15分鐘）→收成→工廠倉庫正確顯示數量→全部賣掉→遊戲幣正確入帳；也驗證了上述兩個 bug 修正後的正確行為（倒數計時不再瞬間完成、同一台機器兩個並發請求只有一個成功）。測試用的假資料（設計圖、生產批次、庫存、遊戲幣帳本紀錄）皆已清空。**已 push。**

### 工廠：設計圖庫支援純文字佔位（2026-07-31 補充）

- Peggy 提出還沒找到可商用圖片前，想先用文字代替，於是 `factory_designs.storage_path` 改為允許 null，前端（新增 `components/factory/DesignThumb.tsx`）沒有圖片時自動改顯示文字色塊。`scripts/add-factory-design.mjs` 新增 `--text "顯示文字"` 用法可直接新增文字佔位設計，原本上傳圖片的用法不受影響、可混用。
- 已在正式資料庫加入 4 筆文字佔位設計（貓咪／兔子／星星／愛心）供 Peggy 先體驗完整流程，等找到真正素材後可以再用原本圖片上傳用法補上，兩者不衝突。

### 工廠：排隊生產（2026-07-31 補充，依 `idea/生產鏈.jpg` 手繪 wireframe 改版）

- **背景**：v1 原本設計是「同一台機器同時只能一批生產中，做完才能排下一批」，Peggy 提出真正的玩家可能在上班，沒辦法每 10~30 分鐘就開一次遊戲收成再排下一批，希望改成可以一次排好幾批、機器依序自動生產，玩家隔一段時間再回來就能一次收到好幾批成品。
- **定案設計**：同一台機器同時只有一批「正在跑」，但允許同時排最多 `MAX_QUEUE_PER_MACHINE`（`lib/factory/catalog.ts`，目前是 4）批在佇列裡；新排的一批，生產時間從「佇列裡最後一批預計完成的時間」開始算（如果佇列最後一批其實已經到時間、只是玩家還沒收成，新的一批改成從現在開始算，不會平白繼承一段已經過去的等待時間）。玩家 40 分鐘沒開遊戲，回來時可能同時看到好幾批都已完成、等著收成，不用一直盯著手機。
- **UI 調整**（`components/factory/MachineCard.tsx`）：格式按鈕直接標示產出數量（例如「海報 4件」）；機台卡片改成先列出目前佇列裡的每一批（各自顯示倒數或收成按鈕），佇列未滿時才在下面顯示選格式＋選圖＋確認文字＋「製作」按鈕；佇列滿了（4/4）改顯示提示文字，不能再排新的。
- **同一台機器最多排 4 批的上限，這次故意不做成資料庫層的硬性約束**（跟先前「同一時間只能一批」不同，那個有做資料庫 partial unique index）：因為現在同一台機器本來就允許多筆同時 `in_progress`，就算極端情況下因為併發請求多排到 5、6 批，頂多是佇列比預期多一點，不是資料損毀或能白拿獎勵的問題，嚴重度遠低於先前那個 bug，值得用比較簡單的應用層檢查換取開發時間。
- **已驗證**：`tsc`／`lint`／`build` 皆通過；本機測試帳號依序排了海報→明信片→小卡→小卡四批，查資料庫確認四批的 `ready_at` 精確依序間隔 15 分鐘（印表機的生產時間），畫面上四個佇列格分別顯示正確倒數；排滿 4 批後畫面正確改顯示「排隊已滿」、選圖表單消失；測試資料已清空（保留文字佔位設計圖）。**已 push。**
- `/town` 頁面的「工廠」建築已從灰階佔位改為可進入，Header 導覽列也新增工廠連結。
- **上線後 Peggy 實測抓到的顯示 bug（2026-07-31，已修正）**：排在佇列後面的批次，畫面原本會直接顯示「距離這批真正完成」的累加剩餘時間並跟著即時倒數（例如印表機第一批還剩15分鐘時，第二批一開始就顯示剩30分鐘、而且兩個數字同時在跳），這會讓玩家誤以為兩批同時在生產。後端排程數字其實是對的（第二批本來就得等第一批做完才開始算），只是畫面呈現方式讓人誤解。修正為：只有真正「輪到它」的那一批（前一批已經到 `ready_at`）才顯示即時倒數，還在等待的批次改顯示靜態文字「排隊中・輪到它開始生產後約需 N 分鐘」，不跟著跳動，等前一批完成後才會換成即時倒數。
- 新增 `scripts/dev-grant-currency.mjs`（可用 email 或 user_id 查找），供 Peggy 之後自己測試工廠功能時發放測試遊戲幣用，比照第10-1項「作弊能力只留在本機腳本」原則。

### 工廠：視覺動畫骨架（2026-08-01，placeholder-first 架構）

- **背景**：Peggy 帶了 AI 影片生成的參考素材（`idea/印表機.mp4` 拆成 `印表機1~5.png`），想像中的效果是「待機（空白紙捲）→運作中（印花紋）→output（印好的紙滑出）→打包裝箱→卡車運去超市」五段式敘事。確認過那個 3D 玻璃質感等級的美術是 Claude Code 生不出來的（呼應第11-1項），美術素材之後由 Peggy 自己用 Gemini Pro 生成，Claude Code 負責把素材串起來動；此為兩邊並行開發的第一步：Peggy 去手動測試既有工廠邏輯，Claude Code 同步先把不依賴美術的「動畫骨架」搭好。
- **新增 `components/factory/MachineScene.tsx`**：把工廠敘事收斂成 5 種狀態的型別 `MachineStage = 'idle' | 'processing' | 'ready' | 'collecting' | 'shipped'`，目前用 emoji + Tailwind 動畫（`animate-pulse`/`animate-bounce`/`animate-spin` + 進度條）當佔位視覺，四台機器（印表機/裁縫機/壓模機/雷雕機）在 idle/processing/ready 三階段各自有對應的 emoji 區隔（例如印表機是 🧻→🖨️→📄）。**這個元件就是素材插槽**：之後 Peggy 從 Gemini 拿到真的圖，只需要把 `STAGE_ICON` 換成 `<img>`，不用動呼叫端或狀態判斷邏輯。
- **`components/factory/MachineCard.tsx` 整合**：在每張機台卡片頂端加一個 `MachineScene`，狀態來源是佇列最前面（正在跑的）那筆批次：用 `started_at`/`ready_at` 算 processing 階段的進度百分比；按下「收成」時本地覆蓋成 `collecting`→（API 成功後）`shipped` 1.5 秒再淡出，之後恢復由新的佇列狀態接手顯示。不影響、也沒有更動原本的排隊/倒數/收成互動邏輯（那些已經測過的功能刻意不碰）。
- **新增 `scripts/dev-fast-forward-batches.mjs`**：把指定帳號所有生產中批次的 `ready_at` 撥到現在，測試「運作中→已完成」的視覺切換不用真的等 10~30 分鐘，沿用第10-1項「作弊能力只留在本機腳本」的既有模式。
- **已驗證**：`tsc --noEmit`／`lint`／`build` 皆通過。**尚未用真實帳號跑過瀏覽器視覺驗證**——Peggy 這次會親自用既有測試帳號在 `/factory` 操作驗證邏輯是否符合預期，順便看這版動畫骨架的視覺效果，此為本次刻意的分工（見 `idea/開發日誌.md` 2026-08-01）。
- **開發測試用生產時間縮短（同日補充）**：`lib/factory/catalog.ts` 新增環境變數 `FACTORY_DEV_PRODUCTION_MINUTES` 覆寫，只設在 `.env.local`（正式站 Vercel 沒有這個變數，不受影響），設成 1 之後四台機器不管哪個格式都變成 1 分鐘生產完成，Peggy 測試時不用每次跑腳本或等真實時間，呼應第10-1項測試策略選項(b)。

### 超市系統 v1（2026-08-01 開發完成，對應第32項）

- **新增資料庫物件**（`supabase/schema.sql`，已套用到正式資料庫）：`market_shelves`（貨架，記錄 `capacity` 總件數上限）、`market_shelf_slots`（貨架上的商品格，`active_from` 在上架當下算好、之後不變動，賣了多少純靠讀取時用時間差推算，不跑背景排程）；`public.users` 新增 `warehouse_capacity`（工廠倉庫容量上限，預設300）；`handle_new_user()` trigger 補上新手起始資源（150幣+1個免費貨架），解決拿掉即時收購後的雞生蛋問題——**這個起始贈禮只對新註冊帳號生效，既有帳號不會回溯拿到**。
- **經濟參數集中在 `lib/market/catalog.ts`**（草案數字，可調）：買貨架150幣、貨架容量10件、倉庫升級500幣／次+50容量、新手起始150幣+1貨架。
- **貨架賣出邏輯**（`lib/market/catalog.ts` 的 `computeSlotRemaining`/`computeSlotSoldSoFar`/`isSlotActive`/`minutesUntilSoldOut`）：跟工廠生產佇列同一招，`active_from` 上架當下算好（接在同貨架排隊中最晚預計賣完的時間之後，貨架前面沒東西賣就從現在開始），讀取時純算時間差，不需要背景排程。同貨架多商品先進先賣完，不輪流。
- **貨架容量以總件數計算（2026-08-01 修正，取代原本「最多幾種商品」的設計）**：`market_shelves.capacity` 是總件數上限，不限制能放幾種商品；`app/api/market/list` 用 `computeSlotRemaining` 加總「現在架上實際還剩幾件」（不是上架當下的原始件數）來判斷還有沒有空位，所以賣掉的東西會即時釋放空位，不用等整批賣完+收款才空出來。原本 `slot_index`／「固定幾格」的欄位跟概念已經整個拿掉。
- **API 路由**（皆用 service role 執行，比照工廠 API 的既有模式）：`app/api/market/buy-shelf`（買貨架）、`app/api/market/list`（從工廠倉庫上架到貨架，檢查總件數空位夠不夠）、`app/api/market/delist`（下架，先結算已賣未收款部分再退回倉庫）、`app/api/market/collect`（收款，帳號層級一次結算所有貨架，用「推算已賣數量 - `collected_quantity` 已入帳數量」算差額入帳，同一時間按兩次差額是0，不會重複入帳）、`app/api/factory/upgrade-warehouse`（升級倉庫容量）。**移除了原本的 `app/api/factory/sell`**（舊的「全部賣掉」即時收購，功能被超市取代）。
- **工廠倉庫容量上限**：`app/api/factory/collect` 收成前檢查會不會超過 `warehouse_capacity`，超過就擋下來並提示去超市上架或升級倉庫。`components/factory/Warehouse.tsx` 改成純展示（拿掉賣出按鈕），顯示容量 X/Y，附連結導去 `/market`。
- **頁面**：新增 `app/market/page.tsx` + `components/market/{BuyShelfButton,ShelfCard,RevenuePanel,MarketOpenToggle}.tsx`。`ShelfCard` 負責單一貨架的格子顯示（剩餘/售罄狀態、下架按鈕）跟上架清單（2026-08-01 依 `idea/上架系統.jpg` wireframe 重做：不再是「每個空格一張表單」，改成列出工廠倉庫所有可上架品項，勾選一項＋輸入要上架幾件＋按上架，貨架標題顯示「空位 X/Y」即時反映剩餘總容量）；`RevenuePanel` 是帳號層級（不分貨架）的「本日營業額」面板，即時顯示目前累積多少還沒入帳、可展開「結算明細」（純前端算給玩家看，不呼叫API）看各商品品項的賣出數量與金額，按「入帳」才真正呼叫 `/api/market/collect` 把錢存進遊戲幣、清空營業額。`/town` 的超市建築移出灰階佔位清單，指向 `/market`；Header 導覽列新增「超市」連結。上架動作確認放在 `/market` 頁（不放 `/factory`），理由見上方已定案項目32。
- **營業中／暫停營業（2026-08-01 補充）**：`public.users` 新增 `market_open`（預設true）與 `market_closed_at` 兩欄；新增 `app/api/market/toggle-open`，暫停時只記錄 `market_closed_at`，前端（`ShelfCard`/`RevenuePanel`）暫停期間改用這個凍結時間點取代真實現在時間去算剩餘/營業額，倒數看起來就是真的停住；重新營業時，路由用「現在 - market_closed_at」算出暫停了多久，把使用者名下所有貨架格子的 `active_from` 一次整批往後推移同樣的時長，讓「暫停期間不算數」這件事延續到重新營業之後，不需要另外記錄多段暫停區間、也不用在每個讀取點都判斷「現在是不是暫停中」。玩家暫停期間仍可正常上架/下架（`list`/`delist` 路由不檢查營業狀態，暫停只影響賣出的時間推算）。**已知的極小瑕疵（v1接受）**：如果玩家在暫停期間新上架一筆，因為排隊時間是用當下（尚未平移過的）既有格子時間去接續計算，重新營業後這筆新品的實際開賣時間可能會比預期晚個幾分鐘，屬於整批平移公式在這個邊界情況下的近似值，不影響其餘格子/其餘情況的正確性，先不特別處理。
- **已驗證**：`tsc --noEmit`／`lint`／`build` 皆通過；本機 dev server 確認 `/market`、`/factory`、`/town` 未登入時皆正確 307 導向登入頁。**尚未用真實帳號完整跑過買貨架→上架→賣出→結算明細→入帳→下架→暫停/重新營業→倉庫爆倉這條完整流程**，開發過程中一度因為手動清除 `.next` 快取跟 dev server 同時運作導致本機出現短暫 500 錯誤，已排查並排除（跟程式邏輯無關，重啟 dev server 即恢復正常），下一步待 Peggy 實際測試。
- **v1 刻意不做**：貨架的空間佈置視覺化（網格拓寬、買家具、相框/書櫃/洞洞板、牆壁vs地板擺放）目前只有列表 UI，等賣貨機制本身測過覺得好玩，再沿用收納冊已驗證的點選式互動模式做視覺化版本（見第32項）。上架時不顯示成本、不能自訂售價（v2 才考慮開放拖拉調價）。
- **Peggy 真人測試抓到的問題與後續調整（2026-08-01，同日第四、五輪）**：
  1. 已售完的格子不再顯示（原本會留著顯示「已售完」+「下架」按鈕，但賣完的東西沒有實體可以下架，容易誤導，改成賣完直接從畫面消失，見 `ShelfCard.tsx` 的 `SlotView` 過濾邏輯）。
  2. `/market` 頁新增「去工廠繼續生產」連結，不只靠導覽列。
  3. 每張貨架標題旁新增純顯示（無互動）的營業狀態小標籤：綠色「販賣中」／黃色「商店關閉中」，避免玩家忘記自己按過暫停。
  4. Peggy 懷疑賣出計時沒有真的在跑，Claude 直連正式資料庫核對 `active_from`/`quantity` 的實際數字，確認排程計算完全正確（每個 slot 之間的間隔精確對應各自數量的分鐘數），研判她看到「數量沒變」的當下很可能是超市剛好處於暫停營業狀態（上面第3點的狀態標籤就是為了解決這個容易搞混的情境）；已請 Peggy 對照新加的狀態標籤重新確認，如果標籤顯示「販賣中」但數量仍不動才需要再往下查。
  5. 上架清單改版：每一列（不用先勾選）都有自己的輸入框+上架按鈕，輸入框預設值＝`min(該項庫存, 貨架目前剩餘空位)`，貨架空的時候各品項各自顯示自己的庫存量，上架東西後其他品項的預設值自動跟著降到剩餘空位（玩家手動改過的值不會被蓋掉）。呼應 Peggy 提出「預設玩家通常想整批上架」的觀察。
  6. **設計圖庫版權疑慮（重要，尚未解決）**：Peggy 想用自己實體收藏（進擊的巨人官方もちころりん系列公仔）的照片當工廠圖庫素材，原圖 `idea/小公仔原圖.jpg`；Claude 判斷拍照方式本身合乎第2項「拍自己的立體實物算低風險」的標準，但工廠圖庫是**所有玩家共用的量產範本**，跟第2/17項原本評估的「個人收藏頁只有自己看得到」性質不同，角色版權不會因為是自己拍的就不算數，建議這5張先不要放進工廠圖庫（僅工程判斷，非正式法律意見）。連手繪版本也一併討論過：「自己畫、不是描圖」不代表不侵權，版權保護的是角色設計本身的表現，跟創作技法無關（同人圖法律上一樣算衍生創作，只是版權方通常不追究），這點釐清後 Peggy 決定先整理成文字問題請律師朋友確認。**目前這5張圖沒有上傳，工廠圖庫仍是原本的4筆文字佔位（貓咪/兔子/星星/愛心）**，等法律諮詢有結果或 Peggy 決定找別的可商用/原創素材再繼續。
  7. **顯示重複bug修正**：貨架格子原本同時顯示「品項 × 上架當下的原始數量」跟「剩 X 件」兩個數字，容易誤會（例如上架5個、賣掉3個後，畫面同時出現「×5」跟「剩2件」）。改成只顯示品項名稱+「剩 X 件」，不重複標原始數量；還在排隊、尚未輪到開賣的格子維持顯示「共 N 件」。
  8. **入帳併發安全補強**：`/api/market/collect` 對每個 slot 的更新/刪除加上樂觀鎖條件（`.eq('collected_quantity', slot.collected_quantity)`），確保兩個幾乎同時送出的入帳請求不會把同一筆賣出的錢算兩次入帳。這是第31項已知風險（工廠併發競態）的同類問題在超市這邊的補強，工廠那邊維持原本「已知風險、暫不處理」的判斷不變（後果只是佇列數量誤差，不涉及遊戲幣正確性，嚴重度不同）。
  9. **新增導覽列動態提醒欄（回應 Peggy 提出的主動提醒需求）**：新增 `app/api/notifications/summary/route.ts` + `components/Header.tsx` 輪詢這支API（每45秒一次），有東西待處理時在導覽列下方顯示一條淺色提醒欄，點擊可直接跳工廠/超市。**刻意的設計限制（Peggy 明確要求）**：只在網頁畫面內顯示，不使用瀏覽器 Notification API、不改分頁標題、不改 favicon——因為玩家可能在上班時間玩，不能讓提醒用瀏覽器層級的方式（工作列閃爍、分頁標題變化）洩漏到分頁外，只有真的點開這個分頁在看畫面才看得到提醒。這跟第12-1項「不主動推播催促」的收藏庫原則不衝突：那條原則針對的是誘發衝動消費焦慮的情境，工廠/超市這種放置類遊戲玩家本來就預期、也需要「東西好了」的提醒，Peggy 已經明確確認要做這個功能，不是灰色地帶。
  10. **提醒欄邏輯簡化（2026-08-01 依 Peggy 實測回饋調整）**：第一版是算「幾個貨架商品賣完」，但這個數字是照 `market_shelf_slots` 的列數算，跟結算明細（照商品格式分組顯示）算法不一樣，同一批明信片如果拆成兩筆不同設計圖上架，明細顯示「明信片×7」是1行，但提醒欄數的是「2列」，Peggy 實測發現兩邊數字對不起來。改成 Peggy 指定的簡化版：`shelvesNeedingRestock`（有空位可以上架的貨架數，不細算賣了什麼）+ `warehouseEmpty`（工廠倉庫完全沒庫存時提醒該去生產），拿掉容易兜不攏的逐項計數。
  11. **下架排程遞補bug修正**：Peggy 實測抓到，上架明信片8個排第一、娃娃2隻排隊等待，2分鐘後下架明信片，娃娃應該遞補成「現在就開賣」，但畫面仍顯示「排隊中」。原因：下架只處理了被下架的那筆，沒有連動調整排在它後面的格子的 `active_from`（那些值是上架當下就算好、之後不會自動變的，見已定案項目32說明）。修法：`app/api/market/delist` 算出這筆下架格子「原本排定的完整結束時間」減去「現在或原本開始時間取較晚者」得到省下的時間，把同貨架排在它之後（`active_from` >= 這筆的）的所有格子一起往前移動這段時間，讓它們正確遞補。
  12. **提醒欄門檻修正（`idea/bug1.png`）**：「貨架空了」原本只要貨架還有剩餘空位（不管有沒有商品在賣）就會提醒，變成一條永遠掛著的訊息（貨架幾乎不可能剛好100%塞滿）。改成只有貨架**完全空的**（一件商品都沒上架）才提醒，跟「工廠倉庫空了」用同一套判斷標準。生產完成的提醒也拿掉數字，改成固定文字「有商品完成，快去收成」，不寫幾批。
  13. **新增「一鍵收成」（`app/api/factory/collect-all`）**：`/factory` 頁最上面新增按鈕，把所有已完成、還沒收成的批次依完成時間由舊到新一次收成，遇到會超過倉庫容量的那一批就停下來（後面更晚完成的也一併不收，維持先完成先收的順序），回報「已收成 N 批」，如果有批次因為倉庫滿了沒收到，額外提示「已超過倉庫容量，請先上架商品，或批次收成」。
- **Peggy 換電腦測試後回報三項（同日第九輪）**：
  1. **拿掉「二創」字眼**：`/factory`、`/town` 說明文字裡的「二創」移除（改成「生產屬於你的周邊」「生產周邊、賺遊戲幣」），避免文案主動提示玩家這裡可以放侵權二創內容，降低風險。
  2. **生產序列標示設計圖名稱**：原本機台運作動畫跟佇列格子只顯示「海報 × 4」，同格式不同設計圖（例如貓咪海報 vs 星星海報）看不出差異。`MachineScene`／`MachineCard` 的 `QueueSlot` 都補上設計圖名稱＋小縮圖，變成「貓咪・海報 × 4」。
  3. **貨架新增「自動上架」（依 `idea/貨架補滿.png`）**：`ShelfCard` 標題「空位 X/Y」旁新增按鈕，依待上架清單目前顯示順序由上到下自動補滿剩餘空位——最上面品項庫存夠就整批扣滿空位，不夠就扣完換下一項繼續補，省去玩家自己湊數字。沿用既有 `/api/market/list` 逐筆依序呼叫，沒有另外做批次API。
  - `tsc`／`lint`／`build` 皆過，Peggy 已在本機 dev server 測試確認三項都正常，**已 commit + push**（commit `3693873`）。

### ⚠️ 暫時性：正式站生產時間目前被強制改成 1 分鐘（2026-08-01）

- **原因**：Peggy 要換到另一台電腦測試（原本的1分鐘加速只在本機 `.env.local` 生效，正式站不受影響），為了跨裝置測試方便，`lib/factory/catalog.ts` 底部加了一段暫時性的強制覆寫，把**所有機器所有格式**的生產時間都設成1分鐘，**這會影響正式網站上所有訪客**，不是只有 Peggy 自己看得到。目前網站沒有其他真實玩家在用，風險可接受。
- **這是本次連同整套超市系統一起第一次 push 到正式站**：在這之前，2026-08-01 當天做的所有東西（超市系統、工廠動畫骨架、排隊遞補修正、提醒欄、一鍵收成……）都只存在本機，從沒 commit 過，這次是累積一整天的份量一次性推上去。
- **測試完務必記得刪掉** `lib/factory/catalog.ts` 裡標示「⚠️⚠️⚠️ 暫時性設定」的那個 for 迴圈區塊，讓生產時間恢復成 `FACTORY_MACHINES` 裡寫的真實數值（15~30分鐘），再重新 commit + push 一次。
- **2026-08-01 補充**：Peggy 確認第九輪三項修正測試沒問題並已 push，但明確表示**還要繼續測很久，先維持 1 分鐘不要改回去**——這個暫時覆寫還會再留一陣子，不是遺漏忘記處理，之後她測完會主動提出再一起改回真實數值＋push。
- **2026-08-02 再次確認延長**：Peggy 明確表示**至少未來好幾天都要維持 1 分鐘**，因為等 Gemini 生成的工廠美術素材（見 `idea/gemini-工廠美術prompt.md`）換上去之後，還需要能快速反覆測試視覺效果，不是短期內就會改回來。**這不是遺漏，之後她會主動提出才需要改回真實數值＋push**，中途不要自作主張改回去。

### CSV 匯入結果（2026-07-28）

- 成功匯入 400 筆，0 筆內容規則警告（代表先前人工清理過的資料品質不錯）。
- **2 筆因來源 CSV 本身欄位錯位被自動跳過，尚未匯入**（不是腳本的 bug，是 `products.csv` 這兩列資料本身在 character_aliases 之後少打了值，導致後面欄位全部錯位）：
  - 第41列「立體機動裝置 1/12 比例模型」（扭蛋 進擊的巨人～立體機動裝置～系列）
  - 第66列「立體機動裝置 水壺」（USJ 日本環球影城～進擊的巨人THE REAL～系列）
  - 修法：打開 `products.csv` 找到這兩列，把 manufacturer/official_price 欄位補上正確值（原始資料看起來分別是 BANDAI/¥500/2021-11-20 與 USJ/¥3200/2017-01-13），改好後重跑 `npm run import:products` 即可（腳本有重複偵測，其餘400筆不會被重複匯入）。

### product_code 商品編號欄位（2026-07-30 新增）

- 背景：Peggy 打算之後逐行校對 `products.csv`，校對時可能順手修正錯字，若還是靠「系列+商品名稱+賞別」文字比對資料庫既有資料，改字後會對不上、被誤判成新商品。因此在校對開始前先建立穩定的編號比對機制。
- `products` 表新增 `product_code int unique` 欄位（`supabase/schema.sql`），已套用到正式資料庫。
- 執行一次性腳本 `scripts/backfill-product-codes.mjs`，依「系列+商品名稱+賞別」比對，把資料庫既有 400 筆商品回填對應的 CSV 商品編號：**400 筆全部成功回填、0 筆遺漏**（另 2 筆是上面提到的欄位錯位、尚未匯入的舊資料，之後修正重新匯入時會自動帶入編號，不需要另外處理）。
- `scripts/import-products.mjs` 已同步更新，之後新匯入的商品都會一併寫入 `product_code`，但**目前腳本仍只會「新增」，沒有「依 product_code 覆蓋更新既有資料列」的功能**——這一步等 Peggy 完成第一輪校對後再實作（見「已知缺口」最後一項）。

### 玩家互助補圖功能 v1（2026-07-30 開發完成，對應第30項）

- **新增資料庫物件**（`supabase/schema.sql`，已套用到正式資料庫）：`products.official_photo_path`（公版代表照路徑，與僅供後台使用的 `image_url` 明確分開）；新表 `product_photo_submissions`（玩家提交的候選圖，pending/approved/rejected）；新表 `game_currency_ledger`（遊戲幣帳本，見第10-1項，只累加不設餘額欄位，approve 時寫入 +10）；新增兩個 Storage bucket：`product-photo-pending`（private，候選圖）、`product-photos`（public，正式公版圖）。
- **玩家提交流程**：`/products/[id]` 只在該商品 `official_photo_path` 為空時顯示補圖表單（`PhotoContributionControls.tsx`），重用既有的「實體拍攝提醒＋授權勾選」介面規則；使用者可看到自己提交的審核狀態、可撤回待審中的提交。
- **審核（沿用「不做後台UI」原則，見第18/20項）**：三支本機腳本，皆用 service role 直接操作、不透過任何網頁介面：
  - `scripts/approve-photo-submission.mjs <submission_id>`：把檔案從 pending bucket 搬到 public bucket、寫入 `products.official_photo_path`、標記 approved、寫入 `game_currency_ledger` +10。
  - `scripts/reject-photo-submission.mjs <submission_id>`：標記 rejected、刪除暫存檔案。
  - `scripts/set-official-photo.mjs <product_id> <本機圖片路徑>`：Peggy 自己上傳時直接跳過審核流程（見第30項：站長本人不需要審核自己）。
- **公開顯示**：`/products/[id]` 頂部與 `/browse`／`/wishlist` 的 `ProductCard` 縮圖皆會顯示 `official_photo_path`（`lib/supabase/storage.ts` 的 `getOfficialProductPhotoUrl`，純字串組合公開網址，不需要簽名、不需要 Supabase client，Server/Client Component 皆可直接呼叫）。
- **已驗證**：`tsc --noEmit`／`lint`／`build` 皆通過；本機 dev server 實際跑過完整流程並確認後清空測試資料——`set-official-photo.mjs` 上傳真實圖片後 `/products/[id]` 與 `/browse` 都正確顯示縮圖；模擬一筆待審提交後跑 `approve-photo-submission.mjs`，確認 `official_photo_path`／提交狀態／遊戲幣帳本／暫存檔案清除四項都正確；另模擬一筆跑 `reject-photo-submission.mjs`，確認狀態改為 rejected 且暫存檔案被刪除。**2026-07-31 確認：commit `e8fd5ff` 已 push 到 GitHub main 並成功部署到正式站（Vercel Production 顯示 Ready），用瀏覽器實際訪問 `/products/1` 確認補圖提交表單正常顯示。**

### 部署踩雷記錄（2026-07-28，供之後參考）

Vercel 專案 `qa-clinic-taiwan/collector-village` 是沿用舊靜態佔位頁時代建立的專案，**Build and Deployment 設定裡 Framework Preset 卡在「Other」**（沒有自動偵測成 Next.js）。第一次 push 新 Next.js 專案上去時，Vercel 用「Other」模式建置，結果整個網站每個路徑都回應 Vercel 平台層級的 `404 NOT_FOUND`（不是我們 App 的 404 頁，是 Vercel 直接擋在前面，連 Runtime Logs 都沒有任何紀錄，因為根本沒有呼叫到任何 serverless function）。

排查方式：去 Vercel Dashboard → 專案 → Settings → Build and Deployment，看到 Framework Preset 顯示「Other」而非「Next.js」。
修法：把 Framework Preset 改選為「Next.js」（Build/Output/Install/Dev Command 全部維持不 Override，用 Next.js 預設值即可）→ Save → 回 Deployments 列表對最新的部署按「Redeploy」（不勾 Use existing Build Cache）→ 重新建置後即恢復正常。

**如果之後又遇到網站回應 Vercel 的 `NOT_FOUND`（純文字、非 App 樣式）**，優先檢查這個 Framework Preset 設定，而不是懷疑程式碼或環境變數。

### 資料庫直連工具（2026-07-28 新增）

- `.env.local` 新增 `DATABASE_URL`（Supabase 專案 Settings > Database > Connection string，用 Session pooler 那組），加上 `scripts/run-sql.mjs`（`node --env-file=.env.local scripts/run-sql.mjs <sql檔案>`），Claude Code 之後可以直接執行 schema 變更，**不再需要每次請你手動貼到 Supabase SQL Editor**。
- 用途僅限本機開發時執行 SQL；正式站（Vercel）不需要、也沒有設定這個環境變數。

### 我的收納冊功能（2026-07-28，依 `claude-code-instructions-albums.md` 規格開發）

- 新增 `collection_albums`／`album_pages`／`album_slots` 三張表 + RLS 政策（本人可完全操作，`is_public=true` 時任何人含未登入訪客可讀）。
- **規格書裡 `collection_albums.user_id` 原寫 `INT`，但實際 `public.users.id` 是 `uuid`（Supabase Auth 設計），已修正為 `uuid` 才能正確建表**，供之後對照規格書時留意這個落差。
- 「同一商品不可重複選入同一收納冊」在資料庫層沒有辦法直接用唯一約束表達（`album_slots` 只知道 `page_id`，不知道所屬 `album_id`），改在應用層（`lib/supabase/albums.ts` 的 `assignSlotProduct`）跨頁查詢比對後擋下。
- 公開分享頁的照片簽名網址：訪客未登入、無法用一般 RLS 規則簽出他人私人 Storage 裡的照片，`/albums/share/[token]` 改用 service role（`createAdminClient`）產生簽名網址，前提是已經先用一般權限的 client 確認過 `is_public = true` 才會走到這步，範圍有限縮，不是整頁繞過權限檢查。
- 已用真實測試帳號在本機 dev server 走過一遍：新增收納冊→選 6 格版面→選 2 件商品入格→確認同商品從可選清單消失→移除格子→新增第 2 頁（4格）→翻頁確認資料正確→設公開產生分享連結→無 cookie 的 curl 確認公開頁不需登入可看、且只顯示商品名稱→改回私人後分享連結立即 404→刪除收納冊確認 `album_pages`/`album_slots` 隨 cascade 一併清除→刪除帳號確認收納冊也隨 cascade 清除。全部正常。
- **UI 於同日依使用者手繪 wireframe（`idea/收納冊初版.png`）調整為常駐側邊商品清單 + 點選式放入**（不做拖拉，理由與討論見已定案項目29-1），已重新用測試帳號驗證選入/移除皆正常。

### 首頁文案與 Footer 版權聲明（2026-07-28）

- 首頁（`app/page.tsx`）文案改版：標題/副標、簡介、「現在已經可以做的事」「接下來正在開發」兩欄、結尾號召，皆依使用者提供文字定稿；精選商品區塊與註冊/瀏覽 CTA 保留不變。
- Footer（全站共用）版權免責聲明文字更新為正式版本，小字灰階呈現，不搶眼但每頁可見。

### 已知缺口 / 下一步待辦（依優先順序）

1. ~~修正 `products.csv` 中 2 筆錯位資料並重新匯入~~ **已於 2026-07-31 完成**：修正第41、66列的欄位錯位（manufacturer/official_price 補回 BANDAI/¥500、USJ/¥3200，release_date/tags/image_url/source_url 對應歸位）；查證資料庫發現這兩筆（product_code 40、65）其實已存在且資料正確（id 401、402），推測先前已用某種方式補上，此次修正 CSV 只是讓原始檔案跟資料庫內容一致，重跑 `npm run import:products` 確認全部 402 筆皆判定為既有資料，無需新增。
2. 商品清單頁 pagination 目前每頁 48 筆，資料量變大後可視需要調整。
3. 目前 `collector-village-git-main-qa-clinic-taiwan.vercel.app` 這個分支別名網址有 Vercel Deployment Protection（會導到 SSO 登入頁），主要正式網址 `collector-village-nine.vercel.app` 不受影響，如不需要可忽略。
4. **圖片儲存規劃已定案（2026-07-30）**：大量商品圖片（光巨人可能上千張，之後還有其他IP）不進 git repo（避免 repo 體積暴增、撞 GitHub 檔案大小限制），沿用專案已在用的 Supabase Storage，不用另外接 Google 雲端硬碟。**等 Peggy 實際建立本機審核用資料夾、開始上傳圖片時**，記得把該資料夾加進 `.gitignore`；真正要在網站上顯示的圖片才上傳到 Supabase Storage，網站只存路徑/網址。
5. **products.csv 資料審核流程已定案（2026-07-30）**：Peggy 逐行校對時採「不刪除任何東西，審核確認沒問題的那一列剪下貼到CSV下方新區塊」的做法。**等 Peggy 上傳校對過的 CSV（出現「已審核」區塊）時**，需要幫 `scripts/import-products.mjs` 加上「依 `product_code` 比對、覆蓋更新既有資料列」的邏輯（目前腳本只會新增、不會更新既有資料），且只需處理「已審核」區塊裡的資料，不用重跑整份 CSV。`product_code` 欄位與現有400筆資料的回填已於2026-07-30完成（見上方「product_code 商品編號欄位」小節），這一步的前置準備已就緒。**2026-08-01 補充：此項工作暫緩，Peggy 決定先跟超市系統並行處理，見第32項。**
6. **超市系統 v1 待真人測試（2026-08-01 新增，見上方「超市系統 v1」小節）**：買貨架→上架→賣出倒數→收款→下架→工廠倉庫爆倉擋生產，這條完整流程還沒有用真實帳號在瀏覽器裡走過一次，只做過程式碼層級的檢查（`tsc`/`lint`/`build`）。經濟數字（貨架150幣、倉庫升級500幣/+50、新手起始150幣+1貨架）都是草案，試玩後可能需要調整，集中在 `lib/market/catalog.ts` 一個檔案。