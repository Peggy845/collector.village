# Collector.Village 專案進度文件

> 這份文件用來追蹤整個專案的決策與進度，可以拿給其他 AI 對照討論，也方便自己回顧。
> 更新原則：每次跟 Claude 討論出新結論，就回來更新對應區塊。

---

## 專案總覽

**專案名稱**：Collector.Village
**核心概念**：收藏管理網站，玩家可以勾選自己擁有的動畫周邊商品，一開始以《進擊的巨人》為主軸，未來可擴充其他 IP（含 K-pop）。
**開發者背景**：一人開發，C 語言經驗 6 年，無遊戲開發/繪畫經驗，計畫用 AI 輔助寫程式。
**開發策略**：先用一般對話型 AI（不需要實際跑程式的部分）把架構、內容、規格全部想清楚定案，最後集中請 Claude Code 一次性生成程式碼，藉此壓縮 Claude Code 訂閱月數。

---

## 目前狀態：MVP 核心功能已上線（2026-07-28）

**目前網址**：https://collector-village-nine.vercel.app/ 已經是正式版 Next.js + Supabase 網站（不再是靜態佔位頁），`/browse` 可瀏覽 400 件已匯入的商品資料。`nine` 是 Vercel 自動加的字尾，之後可在 Vercel 專案設定改名。
**本機專案路徑**：`C:\Users\Peggy\Downloads\Collecter.village.code`（Next.js + Supabase，已於 2026-07-28 commit 進 git）

> 詳細的「已完成頁面清單」與「下一步待辦」見文件最下方新增的「## 🖥️ Claude Code 開發進度」區塊，每次跟 Claude Code 討論完新進度都回來更新那一節即可，不需要重看整份規劃文件。

---

## ✅ 已定案項目

### 1. MVP 範圍界定
架構圖裡的城鎮、工廠、超市、算命館、社交系統等等，全部列為「未來擴充／可能不做」，**不在 MVP 範圍內**。
MVP 只做：收藏庫瀏覽 + 勾選擁有 + 篩選 + 收藏進度顯示。

### 2. 版權風險處理策略
- 資料庫**只存文字資訊**（商品名、系列、年份、出版社），不存官方圖片。
- 圖片只允許**玩家自行上傳**自己拍的實體照片，這是使用者生成內容（UGC），法律定位跟直接重製官方圖不同，風險大幅降低。
- 這個決策同時解決了原架構圖裡「侵權」「儲存空間」「商品圖授權」三個卡住的問題。
- **補充確認（2026-07-30）：掃描海報等平面美術品，等同直接複製官方美術，風險比照官網圖片，不能當作「像自己拍照」的低風險替代方案。** 只有拍攝自己實際擁有的立體實物（公仔本體等）才算低風險的使用者自產內容。此原則已反映在上傳照片介面的提醒文字與授權勾選（見第17項）。

### 3. 資料庫欄位設計（已完成 schema，待 Claude Code 建置）
表格：`ips`、`series`、`products`、`users`、`user_collections`
（完整 SQL 定義見下方「待 Claude Code 生成」區塊，已經寫好可以直接拿去用）

商品分類（大分類 → 細分類，依粉絲角度整理，可再調整）：
- 立體公仔類：公仔、娃娃、立牌、抬頭娃、壓克力透卡
- 配戴隨身類：鑰匙圈、吊飾、髮夾、徽章、手機支架
- 包袋類：收納包、收納袋、環保袋、錢包
- 服裝類：衣服、娃娃衣服、外套、風衣
- 生活雜貨類：馬克杯、保溫瓶、紙杯墊、塑膠杯墊、桌墊、雨傘
- 紙製/影像類：海報、掛畫、資料夾、貼紙、色紙、拍立得、相卡、膠捲

版權補充原則：`image_url`、`source_url` **僅供管理者後台使用**，不對一般玩家顯示，用途是方便日後查核資料正確性（例如粉絲回報某商品年份標錯，可以快速連過去核對）或當作手繪參考，不會出現在任何公開頁面，維持跟前面版權策略一致。

一番賞系列命名參考範例（`series.name` 實際填寫格式）：
- 進擊的巨人～各自的決意～
- 進擊的巨人～獻出心臟吧～
- 進擊的巨人～地鳴～

### 4. 技術路線
- 不用遊戲引擎，走「Web 應用」路線：前端框架（建議 Next.js）+ PostgreSQL（用 Supabase 代管，內建登入系統跟圖片儲存）。
- 免費起步：Vercel/Netlify（網站託管）+ Supabase（資料庫+圖片儲存），都先用免費方案，等使用者變多、免費額度不夠再考慮花錢升級或買網域。

### 5. 角色標籤規則（雙層設計）
- **`characters`（主要角色標籤，陣列）**：上架時由自己判斷，商品畫面上出現的每個角色都填入，不因該角色戲份少而省略或簡化成「綜合」。理由：像莎夏這類劇情後期退場、周邊本來就稀少的角色，粉絲更需要靠精準標籤找到每一件商品；反之像里維、艾爾文這種周邊量本來就大的角色，即使某件商品沒標到他們，粉絲也還有很多其他選擇可買，影響較小。這層標籤用於篩選 UI。
- **`character_aliases`（角色別名/暱稱標籤，陣列，選填）**：給角色不同時期/身分的稱呼用，例如「漢吉」「團長漢吉」「戰損漢吉」。此欄位不影響主篩選清單，只輔助搜尋精準度。第一版由自己填寫基本標籤，之後開放粉絲協力補充別名也沒問題。
- 一個商品畫面上有多位角色時（例如徽章、資料夾常見組合：里維+艾爾文+漢吉；艾連+米卡莎+阿爾敏；約翰+莎夏+柯尼），比照上述原則全部列入 `characters`，不用「綜合」帶過。

### 6. 價格格式規則
- 統一存成顯示用字串（TEXT），不拆成數字+幣別兩欄（除非未來要做「依價格排序/篩選」或「總市值統計」功能，才需要拆開存）。
- 日幣：`¥780`；台幣：`NT$250`；非賣品：直接存文字 `非賣品`（不留空，避免跟「資料尚未填寫」混淆）。
- 一律不寫「円」「元」等單位字。

### 7. source_url 現況
- 先前用 Python 爬蟲產生的 source_url 皆未人工核對，判定不可信，先清空。之後查證商品資料時，逐筆手動填入該商品的實際頁面連結（不可用網站首頁代替）。

### 8. 分類擴充（category_group）
- 目前已知六大分類（立體公仔類、配戴隨身類、包袋類、服裝類、生活雜貨類、紙製/影像類）之外，確認新增：
  - **影音類**：DVD、藍光、CD、OST
  - **出版品類**：漫畫、畫集、設定集、雜誌特輯
  - **布娃娃類**（第24次討論新增）：玩偶、絨毛娃娃等（例如もちころりん系列），實測資料中發現此類商品量不少，正式納入第9個大分類
- 此欄位非寫死的列舉值，允許之後查證更多年份/商品時持續新增分類（例如電玩類是否要獨立於影音類，視之後查到的數量再決定）。

### 9. 市價相關需求 —— 不做獨立功能，沿用備註欄位即可
- 原本構想的「市價觀察日誌」（price_watch 表 + enable_price_tracking 開關）**決定不做**，改用 `user_collections.note`（既有的泛用備註欄位）取代。
- 理由：獨立功能等於在 schema／功能設計上明確表態「這是給買賣判斷用的」，容易觸及收藏圈對囤貨轉賣/黃牛行為的敏感觀感；泛用備註欄位不預設用途，玩家想記錄價格趨勢、想寫別的東西都可以，設計者不對任何行為背書。
- 技術上也更簡單：不需要新增資料表、不需要開關設定，玩家自行決定備註格式（例如自己手動寫「2026/01 ¥2200 → 2026/06 成交¥3200」）。
- **已定案**：`owned_status` 新增 `'wanted'` 狀態值，讓尚未擁有、僅觀察/想要的商品也能建立一筆 `user_collections` 紀錄，備註欄位才有地方可寫。

### 10. 虛擬擁有機制討論 —— GK 等高單價商品的「夢幻逸品」設計
- **起因**：GK 等高單價商品是「有錢有閒才玩得起」的收藏品類，一般粉絲買不起、也沒有實體空間收納/展示。使用者聯想到能否讓這類商品在遊戲化層面被「虛擬擁有」——例如透過遊戲內小遊戲/超市賺遊戲幣「買」到虛擬版本，布置在自己的虛擬小屋（構想類似動物森友會玩法）。
- **考量的正反意見**：
  - 支持理由：滿足買不起 GK 的粉絲的收藏慾望與情感投射，玩家自己清楚知道那是虛擬的、不會跟真實擁有混淆，屬於情感需求的延伸而非造假；跟收藏管理的核心精神不衝突。
  - 疑慮：專案先前已明確把「城鎮、工廠、超市、算命館、社交系統」列為「未來擴充／可能不做」（見 MVP 範圍界定），若現在整套遊戲經濟系統（賺遊戲幣、逛超市、蓋房子裝飾）都要做，會推翻「先壓縮規劃、最後一次性交給 Claude Code 生成」的開發策略，範圍大幅增加。
- **最終決定（拆成兩層評估）**：
  1. **虛擬擁有的「狀態標記」本身：現在就低成本納入。** `owned_status` 除了 `'wanted'`，再拆分出 `'owned_real'`（實體真的擁有）與 `'owned_virtual'`（虛擬擁有／夢幻逸品）。介面上先只用篩選標籤或小徽章呈現「虛擬擁有」，不做小屋展示畫面。
  2. **「如何賺到虛擬擁有資格」的遊戲機制（超市/小遊戲/賺遊戲幣/裝飾小屋）：維持列在 MVP 之外**，等核心收藏功能站穩、有使用者基礎後再評估要不要做成動物森友會式玩法。
  - 拆解理由：現在就能記錄「這是我的夢幻逸品，我用遊戲化的方式擁有它」這個情感需求，但不用在 MVP 階段承接一整套遊戲經濟系統的開發成本。
- **後續補充（第21次討論）**：「小屋擺放給其他玩家參觀」本質上是社交/公開展示功能，等同於已排除在 MVP 外的社交系統，因此小屋展示與其牽動的 UGC 內容審核問題（見第 21 項）自然一併延後，不影響現在的 MVP 開發排序，但需在啟動此功能開發前先落實審核機制。

### 11. 網站頁面結構規劃
共 6 個頁面，分兩區：**公開瀏覽區**（不需登入）、**會員專屬區**（需登入），中間以登入/註冊銜接。

**公開瀏覽區：**
- **首頁**：訪客入口，放專案簡介、精選商品、註冊/登入按鈕。MVP 階段可由現有佔位頁改造。
- **收藏庫瀏覽頁**：核心頁面，篩選條件對應 schema：IP、系列（series）、`characters`（角色主標籤）、`category_group`/`category`（大小分類）、`kuji_prize_tier`（賞別）。不需登入即可瀏覽，登入後每張商品卡片會多顯示個人的擁有狀態標記。
- **商品詳情頁**：單一商品完整資訊。登入後可在此切換三種狀態（`owned_real`/`owned_virtual`/`wanted`）、上傳實體照片、寫備註、填入手日期。

**會員專屬區（需登入）：**
- **小鎮總覽（第26次討論新增，登入後首頁）**：主題化的導覽 hub，把既有頁面包裝成「小鎮」的視覺概念。分兩類建築：
  - **已開放建築**（對應既有功能，無新開發成本）：**圖書館**＝我的收藏總覽；**家**＝收藏庫瀏覽＋想要清單。
  - **未來擴充建築**（僅在地圖上顯示、暫不可進入，呼應第10/21項已延後的遊戲經濟系統）：工廠、超市、市集、服飾店。這些建築先做成「看得到、點不進去」的視覺元素，成本低但能立即提供沉浸感與未來路線圖的期待感，底層邏輯不超出 MVP 範圍。
- **我的收藏總覽**（＝小鎮裡的「圖書館」）：個人儀表板，顯示整體收藏統計，並可依系列或角色分別統計。此頁需要的「收藏進度計算邏輯」尚待下一步定案。
- **想要清單**（歸於小鎮裡的「家」）：即收藏庫瀏覽頁篩選出 `owned_status = 'wanted'` 的視圖，不需另外做一套介面邏輯。
- **個人設定**：帳號基本管理（密碼、大頭貼），未來若有功能開關需求可往這裡擴充。

**關於資料夾/路由層級變動的說明**：頁面路由結構屬於低成本、可隨時調整的部分（不像資料庫 schema 涉及既有資料遷移），現在的頁面分層不是最終定案、之後隨時可依需要重新組織，不會有歷史包袱問題。

### 11-1. 美術風格決策流程（分階段處理，非現在定案）
- **背景**：使用者提出「小鎮」視覺概念後，發現無法想像 Claude Code 建置完成後的實際樣貌，也不確定該用哪種美術風格（純平面／2.5D／類3D／像素風等）。
- **關鍵釐清**：Claude Code 能處理的是網頁排版、互動邏輯、CSS 樣式，**不能生成建築插畫等美術素材**——這是兩種不同性質的工作，文字描述無法讓 AI 產生特定風格的插畫。
- **決定的處理方式（分三階段，難度由低到高）**：
  1. MVP 階段先用**純平面、簡潔的圖示風格**，不追求立體/3D質感，可用 AI 圖像生成工具產生風格一致的建築小圖示，不需要專業美術資源。
  2. 中期可視預算尋找接案插畫者，提供 3-5 張風格參考圖（例如玩家提到的摩爾莊園截圖），委託繪製一套建築/圖示。
  3. 若要做到 2.5D 或類3D質感，需要專門的美術資源（等角建築素材、角色動畫），建議待有明確預算/資源後再評估，不適合一人開發者在前期投入。
- **決定不在此時定案最終美術風格**：先用選項1完成可運作的網站雛形，有實際畫面後再評估美術方向是否需要調整，比現在憑空討論風格更有效率。

### 12. 收藏進度計算邏輯規則
- **核心原則：實體與虛擬分開計算，不合併顯示成同一個百分比。** `owned_virtual`（夢幻逸品）不計入核心「實體收藏進度」的分子，避免玩家靠虛擬擁有灌水出失真的收藏度數字。
- **依集合性質分兩種呈現方式，不可混用同一套邏輯**（此為本專案的核心倫理決策，AI與開發者日後都應遵守）：
  - **封閉集合（如單一一番賞系列，分母是官方公告時就固定不變的歷史事實）**：維持百分比呈現，例如「7/10 = 70%」。這種分母穩定可查證，百分比對玩家有真實意義（清楚知道還缺哪一項）。
  - **開放集合（如「某角色的所有商品」「全站總量」，分母是資料庫持續擴充中的估計值，永遠不代表官方實際總產出）**：**禁止用百分比呈現**，改用原始數字，例如「已收藏 312 件（資料庫目前收錄約 2000 件相關商品，持續擴充中）」，並附註「此數字反映目前已知範圍，不代表官方實際總產出」。理由：分母浮動時，百分比會製造「進度倒退」的錯覺（例如熱心玩家補充資料後，分母從2000變2500，原本50%的玩家瞬間變42%，但玩家實際擁有的東西完全沒變少），對開放集合而言百分比是失真且會誤導人的呈現方式。
- **統計範圍分三層**：
  1. 全站總進度（開放集合，用原始數字）
  2. 依系列統計（`series_id`，封閉集合，用百分比）
  3. 依角色統計（`characters` 陣列，開放集合，用原始數字）
- **多角色商品計算規則**：一件商品若 `characters` 陣列包含多個角色（例如里維+艾爾文+漢吉），此商品會完整計入每個角色各自的分母，擁有時也各自的分子 +1，不拆分比例。
- **`wanted` 狀態不計入任何進度分子**，只計入分母，純粹作為玩家個人追蹤清單使用。

### 12-1. 反焦慮／反衝動消費設計原則（重要，AI與開發者皆須遵守）
- **起因**：擔心百分比／收藏數字設計，可能誘發類似「遊戲成就解鎖」的心態，讓部分玩家（尤其未成年或衝動型消費者）為了湊齊進度而做出負擔不起的消費，或引發「誰收藏得多才是真粉絲」的比較心態與排他感（類比：拿演唱會場次數比較粉絲資格）。
- **已決定的防護原則**：
  1. **絕不做「集滿即可兌換／解鎖」機制**——這是典型會誘發過度消費的 gacha 設計，本專案明確排除，日後也不得追加。
  2. **不主動推播「只差一件就完成」之類的催促提醒**——玩家可以主動查詢自己的進度，但系統不得主動製造急迫感。
  3. **不做進度排行榜或玩家間的收藏數比較功能**（呼應 MVP 範圍已排除的社交系統，此處明確訂為原則而非僅是隱含排除）。
  4. **我的收藏總覽頁面用詞**：採用偏中性的「收藏紀錄」而非強調競賽/成就感的「進度」，弱化遊戲化催促感，強化「這是我自己的個人記錄」的語氣。

### 13. 使用者流程圖
主流程五步：**瀏覽收藏庫 → 商品詳情頁 → 標記擁有狀態 → 填寫備註/照片 → 我的收藏總覽**。
- 若使用者在商品詳情頁想標記狀態時尚未登入，導向登入/註冊頁，完成後直接返回原本瀏覽的那件商品繼續操作（避免登入完要重新搜尋一次的挫折感，實作上通常靠登入頁記住 redirect 網址達成，提醒之後跟 Claude Code 溝通時特別點出這點）。
- 「填寫備註/照片」為可選填步驟，勾選擁有狀態即可完成主要動作，備註跟照片是加分項，不強制填寫。

### 14. UI 文字/介面用語整理

**狀態標籤（對應 owned_status）：**
| 資料庫值 | 顯示文字 | 說明 |
|---|---|---|
| `owned_real` | 已擁有 | 綠色系標籤 |
| `owned_virtual` | 虛擬收藏 | 紫色系標籤，與「已擁有」明顯區隔 |
| `wanted` | 想要 | 灰色/外框樣式 |

**主要操作按鈕：**
- 商品詳情頁未標記：標記為已擁有 / 加入想要清單 / 標記為虛擬收藏
- 商品詳情頁已標記：編輯備註 / 上傳照片 / 移除標記
- 收藏庫瀏覽頁：篩選 / 清除篩選 / 依系列瀏覽 / 依角色瀏覽
- 我的收藏總覽：查看詳細進度 / 切換顯示虛擬收藏

**篩選面板標籤（對應 schema 欄位）：**
- 作品（IP，目前僅進擊的巨人一個選項）
- 系列/活動（對應 `series.name`）
- 角色（對應 `characters`，複選）
- 分類（拆兩層：先選大分類 `category_group`，再選細分類 `category`）
- 賞別（對應 `kuji_prize_tier`，僅一番賞商品出現）

**收藏進度顯示文字：**呼應第 12 項規則，「實體收藏進度」為主要顯示，「含虛擬收藏」為次要/可摺疊顯示，兩者不合併成單一數字。

### 15. 專案資料夾結構規劃
走 Next.js（全端框架），非傳統前後端分離的兩個專案，改以「頁面/元件/共用邏輯」三層分工：

```
collector-village/
├── app/                       # 頁面路由，對應6個定案頁面
│   ├── page.tsx               # 首頁
│   ├── login/
│   ├── register/
│   ├── browse/                # 收藏庫瀏覽頁
│   ├── products/[id]/         # 商品詳情頁（動態路由）
│   ├── dashboard/             # 我的收藏總覽
│   ├── wishlist/              # 想要清單
│   └── settings/              # 個人設定
├── components/                # 共用UI元件（商品卡片、篩選面板、狀態標籤等）
├── lib/
│   ├── supabase/              # Supabase client 初始化、資料庫查詢函式
│   └── utils/                 # 共用邏輯，例如收藏進度計算函式（對應第12項規則）
├── types/                      # TypeScript 型別定義，對應資料庫 schema 欄位
├── public/                     # 靜態資源（LOGO、預設圖，不含商品官方圖）
├── scripts/                    # CSV批次匯入工具（後台用，非玩家可見）
├── .env.local                  # Supabase 連線金鑰等環境變數
├── package.json
└── next.config.js
```

- `products/[id]/` 為動態路由，一個資料夾對應所有商品詳情頁，不需逐一開檔案。
- `scripts/` 專放後台/開發者工具，與玩家可見的 `app/` 頁面明確分開。
- `lib/utils/` 存放收藏進度計算等共用邏輯，避免散落在各頁面元件中，方便日後統一調整規則。

### 16. 收藏隱私範圍
- **預設：完全私人。** 登入使用者只看得到自己的 `user_collections` 資料（狀態、備註、照片），其他使用者的收藏內容一律看不到。
- 理由：直接對應第 12-1 項反焦慮設計原則。若收藏資料預設可被他人看到，等於留了後門讓「比較心態」（誰收藏得多才是真粉絲）透過非正式管道發生，與避免比較/焦慮的方向矛盾。
- **例外（列為 MVP 之後擴充，非現在要做）**：使用者可自行選擇產生「分享連結」，傳給朋友看自己的收藏。此連結為唯讀、不出現在任何公開列表、不可被搜尋、不顯示其他使用者的排名或比較——性質上是使用者主動、一次性的分享行為，跟系統驅動的排行榜/比較功能（已在12-1排除）完全不同，兩者不衝突。
- **此項「預設完全私人」需現在定案**，因為這是 Supabase Row Level Security（資料列層級權限）規則的基礎，若晚點才決定，會導致已寫好的權限邏輯需整批修改。

### 17. 照片上傳功能規格
- **儲存路徑結構**：`bucket: collection-photos`，路徑階層為 `user_id/product_id/檔名`（例如 `u123/p456/photo1.jpg`），天然對應第16項隱私規則——只要 bucket 權限設定為「僅該路徑對應的 user_id 本人可存取」，照片隱私就自動與收藏資料隱私一致，不需另外寫邏輯。
- **格式與大小限制**：接受 jpg/png/webp，單檔上限 5MB。**上傳前需在前端先壓縮/縮圖**（例如長邊壓至 1600px 內）——這是讓 Supabase 免費方案 1GB 儲存額度撐久一點的關鍵，不是可有可無的優化，避免玩家直接上傳手機原圖（動輒10MB+）迅速吃光額度。
- **一件收藏一張照片**：MVP 階段維持 `photo_url` 為單一 TEXT 欄位，不做多張照片（如正面/背面）。等實際觀察到使用者有此需求，再評估改為 `photo_urls TEXT[]`，現在不為未驗證的需求增加資料結構複雜度。
- **存取權限**：bucket 設為 private，前端讀取用 Supabase 簽名網址（signed URL）短期授權存取，非公開網址直接可看，避免使用者上傳的實體照片被猜網址存取。
- **上傳提醒與授權勾選（2026-07-30 定案並已實作）**：呼應第2項版權原則（掃描/描圖不算低風險替代方案），上傳照片區塊需以顯著文字提醒「僅限上傳自己實際拍攝的實體商品照片，不可使用官方圖片、海報掃描、電繪／描圖等平面美術重製方式」，並需勾選「我確認這是我自己實體拍攝的照片，並同意本站得於服務範圍內使用、顯示此照片」才能選取檔案上傳，未勾選時檔案輸入框停用。已實作於 `app/products/[id]/CollectionControls.tsx`。

### 18. CSV 批次匯入工具規格
- **決定不做「後台管理介面」，改用本機腳本**：目前規劃裡只有開發者本人會匯入商品資料，不需要多位管理者，若做一套「後台登入+上傳CSV+網頁結果介面」等於多蓋一整套管理者權限系統，對一人開發者是不必要的工程量，直接省下這塊，有助於壓縮 Claude Code 訂閱月數的開發策略。
- **做法**：放在 `scripts/` 資料夾（對應第15項資料夾結構）的本機 Node.js 腳本，開發者在自己電腦執行，用 Supabase service role 金鑰（具完整權限、不受 RLS 限制）直接寫入資料庫，不透過網站介面、不需額外的管理者登入機制。
- **腳本驗證邏輯**：
  1. 必填欄位檢查（`name`、`category`、`category_group`、`ip_id`），缺一則該筆跳過並記錄原因，不因單筆資料不完整讓整批匯入失敗。
  2. 重複偵測：以「系列 + 商品名稱 + 賞別」三者組合判斷是否為重複商品（避免單純比對商品名稱容易誤判），重複時預設跳過並列入報告，由開發者自行決定是否手動覆蓋。
  3. 執行後輸出結果報告（成功筆數、跳過筆數及原因、失敗筆數及原因），方便匯入後快速人工檢查。

### 19. 使用者註冊/登入功能規格
- **MVP 階段只做 Email + 密碼**，不做 Google/Facebook 等第三方登入（OAuth）。理由：OAuth 需額外設定第三方應用程式與授權流程，對 MVP 是增加複雜度換取「登入更方便」的錦上添花體驗，非核心功能；待網站有實際使用者、觀察到註冊步驟造成明顯流失時再評估加入。
- **登入只用 Email，不支援 username 登入**：`username` 欄位保留，但純粹作為玩家自選的顯示暱稱（例如未來若做分享連結功能，顯示用 username 而非 email），登入驗證只認 email，可直接使用 Supabase Auth 內建 email+password 機制，不需額外寫「username 查 email 再登入」的轉換邏輯。
- **Email 驗證信維持 Supabase 預設開啟**：註冊後需點驗證連結才能登入，雖對轉換率略有影響，但能有效擋掉垃圾帳號，對一人開發者而言值得這個取捨。
- **忘記密碼流程使用 Supabase 內建「重設密碼」信件機制**，不需自行開發。
- **社群連結（第21次討論補充）**：個人設定頁可選填 FB/IG/Threads 等純文字連結欄位（`users` 表可加 `social_links JSONB` 或類似欄位），僅供顯示、不涉及任何 OAuth 或第三方 API 串接。
- **分享到社群平台（第21次討論補充）**：不與 FB/IG 做 API 層級串接（需申請開發者權限、長期維護第三方 API 版本更新，對一人開發者是持續負擔）。改用瀏覽器內建的 **Web Share API**，讓使用者透過系統原生分享選單自行選擇分享目的地，維護成本趨近於零。

### 20. 官方商品回報流程（玩家協助回報遺漏項目）
- **對象**：一般玩家（非二創內容），用於回報資料庫中遺漏的正版商品。
- **流程**：介面設定頁提供「回報遺漏項目」按鈕，產生連結導向表單/頁面，讓玩家填寫跟 CSV 相同格式的商品資訊並提交。
- **審核機制**：提交後先寫入獨立的 `product_submissions` 表（欄位同 `products`，外加 `submitted_by`、`status`），**不需自建審核後台介面**，直接用 **Supabase 內建 Table Editor** 查看與核准，核准後用小腳本將資料搬移至正式 `products` 表。此設計避免多開發一套管理 UI，維持一人開發者的省力策略。
- **必須經過開發者本人審核後才會寫入正式資料庫**，避免玩家亂傳或重複/風格不一致的內容污染資料庫。

### 21. 二創 UGC 內容審核原則（未來擴充功能，先定原則）
- **背景**：使用者構想讓二創玩家透過遊戲內「工廠」自製內容並擺放在虛擬小屋展示給其他玩家參觀。此功能歸屬第10項已延後的遊戲經濟系統範疇，非 MVP 範圍，但涉及重大內容安全風險，先在此定案原則，待日後真正開發此功能時遵守。
- **核心判斷（重要，AI與開發者皆須遵守）**：**只要使用者上傳的內容可能被其他玩家看到，審核機制就不是可選項，而是法律責任問題**——不僅是一般成人內容的疑慮，更嚴重的是可能涉及未成年不當內容的風險，經營者即使不知情也可能需承擔法律責任。「公開展示」與「完全不審核」兩者不可並存。
- **已定案的審核架構（供日後實作參考）**：
  1. **上傳時強制先經自動化內容審核 API**（例如 Google Cloud Vision SafeSearch、AWS Rekognition 或同類服務），自動攔截明顯違規內容，被自動標記者才進入人工複查，而非人工逐一審查全部內容。
  2. **任何展示內容均需提供「舉報」按鈕**，任何使用者發現不當內容可一鍵舉報，開發者收到舉報後可立即下架，作為事後補救的最後防線。
  3. **註冊時的服務條款需明確列出禁止事項**（例如禁止上傳露骨/未成年不當內容），並保留開發者隨時停權帳號的權利。
  4. **保守替代方案**：若自動化審核的技術/成本門檻對一人開發者過高，UGC 內容可比照第22項官方商品回報流程，同樣先進入審核佇列由開發者本人核准後才公開展示——雖與「玩家自主生產不需審核」的初衷有落差，但在量能不足時是更安全的預設選項，待使用者規模擴大、有能力建置自動化審核後再開放即時公開。

### 22. 網站版權免責聲明
- **背景**：本站展示大量《進擊的巨人》等版權作品的周邊商品資訊，雖已透過「只存文字資訊、不存官方圖片、圖片僅玩家自行上傳」的策略降低重製風險（見第2項），但網站本身仍應有明確的免責聲明，這是獨立於資料庫設計之外、必須存在於網站頁面上的文字內容。
- **決定**：於網站頁尾（footer）固定顯示免責聲明，內容至少包含：本站為非官方粉絲自製之收藏管理工具，與原作、出版社、玩具製造商無任何關聯或授權關係；所有角色、商品名稱、商標均屬原權利人所有；商品資料僅供收藏管理參考，正確性以官方資訊為準。
- 此聲明文字待網站實際建置時，交由 Claude Code 或後續文案階段定稿，此處先確立「必須要有」與「應涵蓋的重點」。

### 23. 服務條款與使用者年齡政策
- **背景**：本站涉及使用者上傳照片內容（第17項）與未來的 UGC 功能（第21項），且第12-1項已明確關注避免誘導未成年人衝動消費，這些都需要一份正式的服務條款作為法律基礎，並且需要決定最低使用年齡。
- **台灣法規查證結果（2026/07/26 查證）**：台灣《民法》自2023年1月1日起將成年年齡從20歲下修為18歲；7歲以上未滿18歲屬「限制行為能力人」，其法律行為（如簽署契約）須經法定代理人承認才生效。台灣個資法本身**沒有**像美國COPPA那樣針對兒童訂出明確的年齡門檻，一般網路服務常見的「13歲」門檻是國際業界慣例，非台灣法定要求。本站不涉及金流交易，風險層級低於直接金錢交易類服務，但仍有曝險。
- **決定**：
  1. 註冊時需勾選同意服務條款（Terms of Service）與隱私權政策，未勾選不可完成註冊。
  2. **最低使用年齡定為13歲**（業界慣例），並於服務條款中明確加註「未滿18歲之使用者，需徵得法定代理人（監護人）同意方可使用本服務」，呼應民法限制行為能力人規定。
  3. 服務條款需明確列出禁止行為（例如上傳不當內容、騷擾其他使用者等），並保留開發者停權帳號的權利，呼應第21項審核原則。
- **重要提醒（非正式法律意見）**：上述為工程規劃層級的判斷與公開資料查證，非正式法律意見。待照片上傳、UGC功能等實際上線前，建議諮詢律師確認服務條款文字，尤其台灣另有《兒童及少年福利與權益保障法》等規範可能涉及刑事責任層級，超出本文件查證範圍。
- 條款完整文字內容非本文件範圍，待你決定要自行撰寫或請律師/範本協助時再處理，此處先定案「需要有」與「需涵蓋的要點」。

### 24. 帳號與資料刪除權利
- **背景**：使用者應有權利刪除自己的帳號與相關資料（常見的隱私法規要求，例如台灣個人資料保護法、歐盟GDPR等皆有類似規範），這點目前規劃中尚未提及。
- **決定**：個人設定頁（第11項頁面結構）新增「刪除帳號」功能，執行後需刪除或去識別化該使用者的 `user_collections`、上傳照片（Storage 中對應路徑）、`product_submissions` 中的提交紀錄等所有關聯個人資料，並且此操作需要二次確認（例如輸入密碼或打字確認），避免誤觸。

### 25. 網域命名與 SEO 策略
- **網域**：MVP 階段維持免費 Vercel 子網域，不急著購買正式網域。若之後想要的網域名稱被註冊走，屆時換一個名字即可，不影響開發排程。
- **SEO（搜尋引擎索引）**：MVP 階段商品詳情頁設為 `noindex`（meta tag 告知搜尋引擎不索引），只讓首頁與收藏庫瀏覽頁入口被索引。
  - 理由：商品詳情頁會大量出現角色名稱與商品名稱組合，若被 Google 完整索引等同主動提高在搜尋結果的曝光度，雖然資料庫本身已用文字資訊策略降低版權風險（第2項），但曝光度越高越可能被版權方注意到。
  - 現階段目標是服務核心粉絲小圈子，不靠 SEO 衝流量，不索引不影響現階段成長策略。
  - 「先保守、之後再開放索引」比「先開放、之後想收回」風險小很多（Google 收錄速度遠快於撤下速度），確定沒有版權疑慮後可再開放。

### 26. 商品資料處理標準流程（三階段）
- **背景**：實測發現不管用哪個對話型 AI（Gemini Pro、Meta AI等）爬取商品資料，格式可以套用規則、但內容正確性（尤其角色標籤判斷、系列名稱翻譯）仍需人工校對，這是資料本質問題（需要判斷圖片內容/理解規則），非特定 AI 能力優劣問題。
- **標準流程**：
  1. **AI 草稿**：不限工具，任一 AI（Gemini/Meta AI/Claude對話）依本文件規則爬取/整理商品資料為 xlsx/CSV 草稿。
  2. **自動驗證腳本**（待 Claude Code 建置）：檢查草稿是否違反規則，例如：`characters` 欄位是否出現「綜合」字樣、`category_group` 是否用了清單外的值、`series_name` 是否殘留日文原文未轉換、必填欄位是否缺漏、`official_price` 格式是否正確（`¥`/`NT$`/`非賣品`）。此腳本產出「違規清單」供人工快速定位問題，而非逐筆人工肉眼校對。
  3. **人工校對**：開發者依驗證腳本抓出的問題清單，針對性修正，不需要每筆從頭看起。
- 此流程可套用於任何後續新增的商品資料批次，不限單一 AI 工具或單一資料來源。

### 27. 佔位網站已上線
- GitHub repo：`collector-village`（存放靜態 index.html）
- 部署平台：Vercel 免費方案，串接上述 GitHub repo，自動部署
- 目前網址：https://collector-village-nine.vercel.app/
- 內容：純裝飾用佔位頁（假的收藏進度格子 + 訂閱通知表單，表單尚未串接真實後端）
- 名稱說明：「collector.village」是專案暱稱（把 collector_village 的底線換成點的寫法），不是真的網域後綴，不涉及網域搶註問題

### 28. AI 工具分工策略
- 一般聊天型 AI（Claude 對話、或其他）：想法討論、架構規劃、內容整理、文案。
- Claude Code（月費訂閱制，$20/月 Pro 方案即含）：最後階段實際建置專案、寫 API、串資料庫。
- 目前不用 Gemini 做正式資料整理（可靠度疑慮），改為由 Claude 對話 + 使用者自行查證真實商品資料。

### 29. 我的收納冊功能定案（第27次討論）
- **背景**：使用者構想一個把「已擁有」商品用類似實體小卡收納冊的視覺方式呈現的功能（畫面參考：收納櫃種類/名稱下拉選單、格狀商品排列、新增收納冊、左右翻頁），提出手繪 wireframe 討論。
- **與既有規劃的關係釐清**：
  - 這不是取代 `/browse`（收藏庫瀏覽頁，篩選清單式），而是新增一個獨立頁面，讓使用者用「收藏冊」的方式整理、瀏覽自己已擁有的商品。
  - 曾考慮做「小屋保險箱（私人）／書櫃（公開，訪客可逛進來翻看）」的空間視覺化分享方式，但這本質上等於重新做出第10/21項已明確排除在 MVP 外的「虛擬小屋訪問系統」，且「訪客隨意逛」的呈現方式容易誘發第12-1項要避免的比較心態。**最終決定不做**，改用下方簡化版分享機制。
- **分享機制（簡化版，沿用第16項「分享連結」設計）**：
  - 每本收納冊有「公開／私人」開關，預設私人。
  - 設為公開時，系統產生一組唯讀分享連結（不可搜尋、不出現在任何公開列表／探索頁），使用者自行決定要不要把連結傳出去，性質上是一次性、主動的分享行為，跟系統驅動的公開展示／排行榜不同。
  - **明確排除**：不做「瀏覽所有使用者公開收納冊」的探索/廣場頁面；不做小屋空間視覺化訪問系統。這兩點之後若使用者規模明顯成長，可再重新評估。
  - 公開檢視頁只顯示商品圖片與名稱，**不顯示備註、入手日期等個人資訊**。
- **商品放入規則**：只能從自己標記為 `owned_real` 或 `owned_virtual` 的商品中選取放入格子；`wanted` 狀態商品不可放入（尚未實際擁有，無實體卡可收納）。同一件商品可同時放入多本不同收納冊，但同一本收納冊內不可重複放入同一件商品。
- **版面格數規則（套用現實小卡冊固定規格，不做自由拖拉排版）**：
  - 1格（1×1）
  - 2格（1×2 或 2×1，使用者可選方向）
  - 3格（1×3 或 3×1，使用者可選方向）
  - 4格（固定 2×2）
  - 6格（固定 2×3）
  - 8格（固定 2×4）
  - 9格（固定 3×3）
  - 新增頁面時從上述模板擇一套用，不提供任意行列數的自訂排版，降低開發複雜度。
- **資料庫新增三張表**（`collection_albums`、`album_pages`、`album_slots`，完整 SQL 見下方 SQL schema 區塊）。
- **範圍界定**：此功能目前僅完成規劃討論，尚未交給 Claude Code 開發，待整理成具體指令後再排入開發順序。

### 29-1. 我的收納冊：選商品放入格子的操作方式（2026-07-28 補充定案）
- **背景**：功能已依 `claude-code-instructions-albums.md` 開發完成後，使用者提供手繪 wireframe（`idea/收納冊初版.png`）作為原始發想參考，畫面呈現「商品清單常駐在旁邊、可拖拉進收納冊格子」的操作方式，與初版實作（點格子才彈出選單、選完關閉）不同。
- **討論結果**：
  1. **常駐商品清單這個方向採納**：商品清單改為固定顯示在編輯畫面旁邊（桌機為左側欄、手機因版面較窄改為堆疊在格子下方），不再是點格子才彈出、選完就收起來的彈窗形式，瀏覽/比對商品時不用重複開關選單。
  2. **不做真正的拖拉（drag and drop）**：手機/平板用觸控拖拉的操作體感通常不佳（沒有滑鼠 hover 的即時反饋、容易誤觸拖錯格子），而收藏整理這類操作預期會有不少使用者在手機上進行。改維持「先點格子選定目標，再點商品清單裡的項目放入」兩步驟點選流程，桌機與手機體驗一致。之後如果想額外支援滑鼠拖拉當進階捷徑，可以再疊加上去，不影響現有點選流程。
  3. **wireframe 裡其他呈現方式**（用下拉選單切換收納冊、左右兩頁攤開同時顯示、收納冊類型為固定分類按鈕而非自由文字）目前**不採納**，維持原本規格（獨立列表頁 `/albums`、一次顯示一頁＋上下頁切換、`album_type` 自由文字欄位），先以較低開發成本的版本上線，之後有需要再重新評估。

### 30. 玩家互助補圖功能定案（第28次討論，v1 規格）—— 未來擴充功能，尚未開發
- **背景**：起因是 Peggy 打算先自行上傳手邊實體公仔照片，讓早期玩家一進站就看到部分商品已有圖，避免網站看起來像空殼/詐騙站。討論延伸出「開放玩家協助補圖」的完整構想，此處收斂為 v1 定案版本（列為未來擴充功能，非現行 MVP 排程，待之後評估開始開發時再交給 Claude Code）。
- **商品分兩層圖**：
  1. **商品「公版代表照」**：全站瀏覽該商品時看到的圖，初始為空。
  2. **玩家個人收藏頁的照片**：沿用既有已定案機制（第17項），私有、僅本人看得到，不需審核，玩家自行對內容負責。
- **v1 核心簡化規則（關鍵）：只在該商品「完全沒有公版代表照」時，才開放玩家提交候選圖成為公版圖；一旦某商品已經有公版代表照（不論是 Peggy 自己上傳、或其他玩家提交審核通過的），就不再開放其他玩家提交「取代」該公版圖的候選圖——此時其他玩家只能透過第17項既有的個人照片機制更新「自己那端」看到的圖，不影響、也不能覆蓋全站公版圖。**
  - 理由：討論中發現若開放「更好的圖可以取代已核准公版圖」，會出現「自己的圖被換掉」的失落/比較心態（呼應第12-1項反焦慮設計原則，性質上等同變相的排行榜競爭）；同時能把 Peggy 的審核工作簡化成「這張圖能不能用」的二選一判斷，不需要持續評比多張候選圖優劣，也避免同一商品堆積大量候選圖佔用 Storage 空間。
  - 「取代已核准公版圖」機制**目前不做**，列為更之後才評估的擴充；若之後要做，需先想清楚如何避免比較心態（例如公版圖不掛名貢獻者）。
- **Peggy 身為站長兼玩家的身分**：Peggy 自己上傳的圖直接視為公版圖來源，不需要走待審流程——審核的目的是確認內容/版權/是否對應正確商品，Peggy 本人上傳的自己已確定，不需要另外審核自己。
- **審核機制**：比照第20項「官方商品回報流程」既有模式，玩家提交寫入獨立審核佇列表（例如 `product_photo_submissions`：商品ID、提交者、圖片路徑、狀態），**不建管理後台介面**，由 Peggy 透過 Supabase Table Editor 人工核准/駁回，核准後才寫入商品的公版代表照欄位供全站顯示。
- **獎勵機制**：玩家提交的照片經審核通過、正式成為該商品公版代表照後，可獲得 **10 枚遊戲幣** 獎勵。此獎勵設計刻意不依賴「圖片被選為官方圖」的虛榮心／競爭誘因，即使沒有這個誘因，遊戲幣本身就是實質獎賞，也降低了玩家為搶當「官方圖」重複上傳競爭的動機（呼應上述 v1 簡化的理由）。
- **重要前提尚待補齊**：「遊戲幣」作為可累積的虛擬貨幣系統，目前仍屬於第10項已排除在 MVP 外的遊戲經濟系統範疇，尚未設計/建置（例如尚無餘額欄位、發放/扣款邏輯）。此處先定案「補圖通過審核可獲得10枚遊戲幣」這條規則本身，但實際發放邏輯要等遊戲幣系統本身有基礎建設之後才能真正運作，不代表現在就要開發。
- **授權需求**：呼應第23項服務條款，不論提交公版圖候選或私人照片，上傳當下都需明確勾選同意「授權本站於服務範圍內公開展示此照片」，避免版權/授權爭議。

---

## 🔲 待完成項目（可以用對話型 AI 先做完，不需要 Claude Code）

- [x] 《進擊的巨人》商品資料整理成 CSV —— 已產出 `collector-village-products-final.csv`（402筆乾淨資料），可交給 Claude Code 匯入使用。舊試爬資料（Attack_on_Titan_2019_V2_ALL.xlsx 31筆、萬代一番賞.xlsx 61筆）與此次清理過程中移除的19筆有問題資料，皆未納入最終版本，遺漏部分依第20項「官方商品回報流程」交由玩家陸續回報補足即可，不強求一次到位
- [x] 網站頁面結構規劃 —— 見下方「已定案項目 13」
- [x] UI 文字/介面用語整理 —— 見「已定案項目 16」
- [x] 收藏進度計算邏輯規則 —— 見「已定案項目 12」
- [x] 使用者流程圖 —— 見「已定案項目 15」
- [x] 專案資料夾結構規劃 —— 見「已定案項目 17」

---

## 🤖 標記「之後請 Claude Code 生成」的項目

> 這些是需要實際寫程式/建置環境的部分，先在這裡列出規格，等規劃階段全部完成後，一次性交給 Claude Code 執行。

- [x] 初始化 Next.js 專案 + 資料夾結構
- [x] 建立 Supabase 專案，依照下方 SQL schema 建表（`supabase/schema.sql`，含 RLS 政策與 Storage bucket）
- [x] 使用者註冊/登入功能（可用 Supabase Auth）—— 規格已定案（見「已定案項目 19」：Email+密碼、不做OAuth、開啟信箱驗證）
- [x] 商品清單頁面（含篩選功能：依系列/角色/類別）
- [x] 勾選收藏功能 + 收藏進度計算與顯示
- [x] 玩家上傳照片功能（串接 Supabase Storage）—— 規格已定案（見「已定案項目 17」）
- [x] CSV 批次匯入商品資料的後台工具 —— `scripts/import-products.mjs`（`npm run import:products`），已實際匯入 `products.csv` 400/402 筆（2筆因來源資料欄位錯位被自動跳過，見下方說明）
- [x] 部署到 Vercel —— `https://collector-village-nine.vercel.app/` 已是正式版，過程中踩到一個雷見下方「部署踩雷記錄」
- [x] 「我的收納冊」功能（新頁面 `/albums`）—— 規格已定案（見「已定案項目 29」），含 `collection_albums`/`album_pages`/`album_slots` 三張新表、固定版面模板、簡化版分享連結機制，已於 2026-07-28 開發完成並部署上線（見「Claude Code 開發進度」區塊）

### 資料庫 SQL（已定案，可直接交給 Claude Code）

```sql
CREATE TABLE ips (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  type TEXT
);

CREATE TABLE series (
  id SERIAL PRIMARY KEY,
  ip_id INT REFERENCES ips(id),
  name TEXT,
  release_year INT
);

CREATE TABLE products (
  id SERIAL PRIMARY KEY,
  ip_id INT REFERENCES ips(id),              -- 作品名稱
  series_id INT REFERENCES series(id),       -- 所屬系列/一番賞活動名稱
  name TEXT NOT NULL,                        -- 商品名稱（不含系列名，系列名另存 series）
  characters TEXT[],                         -- 主要角色標籤（畫面上出現的每個角色都填，不因戲份少而省略，用於篩選UI）
  character_aliases TEXT[],                  -- 角色別名/暱稱標籤（如 團長漢吉、戰損漢吉），輔助搜尋用，不進篩選清單，允許粉絲後續補充
  category TEXT,                             -- 細分類（公仔、鑰匙圈...）
  category_group TEXT,                       -- 大分類（立體公仔類、配戴隨身類、包袋類、服裝類、生活雜貨類、紙製/影像類、影音類、出版品類、布娃娃類...持續擴充）
  kuji_prize_tier TEXT,                      -- 賞別，例如 'A賞'、'C賞'，非一番賞商品留空
  manufacturer TEXT,                         -- 製造商（萬代、Sega、GoodSmile...）
  official_price TEXT,                       -- 官方定價，存顯示字串，例如 '¥780'、'NT$250'、'非賣品'（不寫「円」「元」）
  image_url TEXT,                            -- 參考圖片網址（僅存外部連結，不下載重製）
  source_url TEXT,                           -- 商品來源網址（官網/店家頁面），需為該商品實際頁面，寧可留空不可放錯連結
  release_date DATE,
  tags TEXT[],                               -- 標籤，例如 一番賞、萬代、最終季、電影、番外
  created_at TIMESTAMP DEFAULT now()
);

CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  username TEXT UNIQUE,
  email TEXT UNIQUE,
  password_hash TEXT,
  created_at TIMESTAMP DEFAULT now()
);

CREATE TABLE user_collections (
  id SERIAL PRIMARY KEY,
  user_id INT REFERENCES users(id),
  product_id INT REFERENCES products(id),
  owned_status TEXT DEFAULT 'owned_real',    -- 狀態值：'owned_real'（實體真的擁有）、'owned_virtual'（虛擬擁有，如買不起的GK等夢幻逸品）、'wanted'（想要清單，尚未擁有）
  owned_type TEXT,
  photo_url TEXT,
  note TEXT,                                 -- 泛用備註，玩家自由填寫（含價格觀察、購入緣由等），僅存於個人資料，不影響其他玩家
  acquired_date DATE,
  created_at TIMESTAMP DEFAULT now(),
  UNIQUE(user_id, product_id, owned_type)
);

-- 以下三張表對應「已定案項目 29：我的收納冊功能」

CREATE TABLE collection_albums (
  id SERIAL PRIMARY KEY,
  user_id INT REFERENCES users(id) NOT NULL,
  name TEXT NOT NULL,                        -- 使用者自訂收納冊名稱，例如「巨人小卡收藏」
  album_type TEXT,                           -- 收納櫃種類，如小卡收納櫃/徽章/海報，純文字分類用途，不限制可用版面模板
  is_public BOOLEAN DEFAULT false,           -- 公開/私人開關，預設私人
  share_token TEXT UNIQUE,                   -- 設為公開時產生的唯一亂碼字串，用於分享連結網址，私人時可為空
  created_at TIMESTAMP DEFAULT now()
);

CREATE TABLE album_pages (
  id SERIAL PRIMARY KEY,
  album_id INT REFERENCES collection_albums(id) ON DELETE CASCADE NOT NULL,
  page_number INT NOT NULL,                  -- 頁碼，決定左右翻頁順序
  layout_type TEXT NOT NULL,                 -- 版面模板：'1','2h','2v','3h','3v','4','6','8','9'（h=橫向/水平排列，v=直向/垂直排列，僅2格3格有方向選擇）
  created_at TIMESTAMP DEFAULT now()
);

CREATE TABLE album_slots (
  id SERIAL PRIMARY KEY,
  page_id INT REFERENCES album_pages(id) ON DELETE CASCADE NOT NULL,
  slot_index INT NOT NULL,                   -- 格子在頁面中的順序位置（由左至右、由上至下）
  user_collection_id INT REFERENCES user_collections(id), -- 對應放入的商品收藏紀錄，可為空表示空格
  UNIQUE(page_id, slot_index)
);

-- RLS 政策方向（實際語法待 Claude Code 依 Supabase Auth 慣例撰寫）：
-- collection_albums：SELECT 允許 user_id = auth.uid() 或 is_public = true；INSERT/UPDATE/DELETE 僅限 user_id = auth.uid()
-- album_pages / album_slots：比照上層 collection_albums 的公開/私人狀態與擁有者判斷（透過 album_id 關聯查詢）
```

---

## 討論紀錄摘要（時間序）

| 日期/次序 | 討論內容 | 結論 |
|---|---|---|
| 第1次 | 架構圖檢視 | 確認核心吸引力，界定 MVP 範圍，指出版權風險 |
| 第2次 | 資料庫設計 + AI分工 | 完成 schema 設計，決定用文字資料庫避開版權問題 |
| 第3次 | Gemini 初步任務 | 決定不採用 Gemini 做正式資料整理 |
| 第4次 | AI 訂閱選擇 | 決定買 Claude Pro（含 Claude Code），先不買 Max |
| 第5次 | 開發策略調整 | 決定先用對話完成所有非程式規劃，最後集中用 Claude Code |
| 第6次 | 佔位網站上線 | 用 GitHub + Vercel（皆免費）部署靜態佔位頁，澄清「collector.village」只是暱稱非網域，Supabase 留待後續串接資料時再用 |
| 第7次 | 商品分類系統 | 依粉絲角度分類清單，整理成大分類/細分類兩層，新增賞別、製造商、圖片連結、來源網址欄位，圖片連結確認只存外部連結不重製 |
| 第8次 | 欄位用途澄清 + 開始頁面結構規劃 | 確認 image_url/source_url 僅供後台管理者使用、不對玩家開放；分類分組方式獲確認；決定先做網站頁面結構規劃，CSV稍後上傳 |
| 第9次 | 上傳試爬 xlsx 商品資料 + 重訂細部規則 | 發現舊資料缺大分類欄位；角色標籤改為雙層設計（characters 主標籤全列出 + character_aliases 別名輔助）；價格格式統一為 `¥`/`NT$`/`非賣品` 字串；source_url 判定不可信全數清空；分類新增影音類、出版品類；市價相關需求決定不做獨立功能，改沿用泛用備註欄位（避免觸及收藏圈對黃牛行為的敏感觀感）；owned_status 新增 'wanted' 狀態值 |
| 第10次 | 虛擬擁有機制討論（GK夢幻逸品） | 考量買不起GK的粉絲情感需求 vs 專案先前排除的城鎮/超市/遊戲經濟系統範圍衝突，決定拆層處理：owned_status 再拆出 'owned_real'/'owned_virtual' 兩種狀態（低成本，現在納入），但賺遊戲幣/逛超市/裝飾小屋等遊戲機制維持排除在MVP之外 |
| 第11次 | 網站頁面結構規劃 | 定案6個頁面，分公開瀏覽區（首頁、收藏庫瀏覽頁、商品詳情頁）與會員專屬區（我的收藏總覽、想要清單、個人設定），想要清單視為收藏庫瀏覽頁的篩選視圖不需獨立介面邏輯；下一步待訂「我的收藏總覽」需要的收藏進度計算邏輯 |
| 第12次 | 收藏進度計算邏輯規則 | 定案實體/虛擬進度分開計算不合併顯示；三層統計範圍（全站/系列/角色）；多角色商品完整計入每個角色分母分子不拆比例；wanted 狀態只算分母不算分子 |
| 第13次 | 使用者流程圖 | 定案主流程五步：瀏覽收藏庫→商品詳情頁→標記擁有狀態→填寫備註/照片（可選填）→我的收藏總覽；未登入時導向登入/註冊後應返回原商品頁繼續操作 |
| 第14次 | UI 文字/介面用語整理 | 定案狀態標籤顯示文字、主要操作按鈕文案、篩選面板標籤命名、收藏進度顯示文字（呼應第12項實體/虛擬分開顯示原則） |
| 第15次 | 專案資料夾結構規劃 | 定案 Next.js 目錄樹（app/頁面路由、components/共用元件、lib/含supabase與進度計算utils、types/、scripts/後台匯入工具），規劃階段清單僅剩商品資料CSV整理待處理 |
| 第16次 | 進度百分比倫理疑慮討論 | 發現「開放集合分母浮動」會製造進度倒退錯覺（熱心玩家補資料後分母變大，其他人進度反而下降），修正規則：封閉集合（單一系列）用百分比，開放集合（角色/全站總量）改用原始數字不用百分比；新增12-1反焦慮設計原則：禁止集滿解鎖機制、禁止催促提醒、禁止排行榜比較功能、用詞改為「收藏紀錄」弱化競賽感 |
| 第17次 | 收藏隱私範圍 | 定案預設完全私人（Row Level Security 基礎規則），「分享唯讀連結」列為MVP之後可選擴充，與已排除的排行榜/比較功能區隔開（主動一次性分享 vs 系統驅動比較） |
| 第18次 | 照片上傳功能規格 | 定案儲存路徑結構（user_id/product_id階層）、格式限制jpg/png/webp單檔5MB、前端需先壓縮縮圖（節省Supabase免費額度）、MVP先做單張照片不做多張、bucket設為private搭配簽名網址存取 |
| 第19次 | CSV批次匯入工具規格 | 決定不做後台管理介面，改用本機Node.js腳本（用Supabase service role金鑰直接寫入，省下管理者權限系統開發量）；定案驗證邏輯：必填欄位檢查、系列+商品名稱+賞別組合判斷重複、執行後輸出結果報告 |
| 第20次 | 使用者註冊/登入功能規格 | 定案MVP只做Email+密碼、不做OAuth第三方登入；username僅作顯示暱稱不用於登入；開啟Supabase預設Email驗證信；忘記密碼沿用Supabase內建重設機制 |
| 第21次 | 上傳萬代一番賞正式格式xlsx；官方回報流程、UGC審核原則、社群分享方式；AI自主補充遺漏項目 | 確認xlsx格式正確套用新規則（待使用者後續複查內容正確性）；定案官方商品回報流程用Supabase Table Editor人工審核、不建管理後台；定案UGC內容審核原則（強制自動化審核+舉報機制+服務條款，因公開展示內容涉及法律責任非可選項）；小屋展示歸類為已延後之社交功能；社群分享改用Web Share API不做OAuth串接，個人設定可選填社群連結；AI主動補充三項先前未提及但重要的項目：網站版權免責聲明、服務條款與最低使用年齡政策（暫定13歲，待法規確認）、帳號與資料刪除權利 |
| 第22次 | 台灣最低使用年齡法規查證 | 查證台灣民法2023年起成年年齡改為18歲，7-18歲為限制行為能力人需法定代理人同意；台灣個資法無COPPA式明確兒童年齡門檻；正式定案最低使用年齡13歲（業界慣例）+服務條款加註未滿18歲需法定代理人同意；明確標註此為工程規劃判斷非正式法律意見，實際上線前建議諮詢律師 |
| 第23次 | 網域命名與SEO策略 | 定案MVP階段不買正式網域，維持免費子網域；商品詳情頁設為noindex避免SEO曝光提高版權風險，僅首頁與收藏庫瀏覽頁入口被索引 |
| 第24次 | 建置提示文件產出+資料處理流程定案 | 產出獨立的claude-code-build-brief.md收斂27項定案內容供交給Claude Code使用；定案商品資料處理標準流程（AI草稿→自動驗證腳本→人工校對三階段），不限單一AI工具 |
| 第25次 | Gemini爬蟲CSV驗證與清理 | 上傳460筆Gemini搜尋成果CSV，執行驗證腳本抓出：28筆重複表頭列、11筆重複商品、3筆欄位錯位、1筆美金報價、15筆系列名稱殘留日文；正式採納「布娃娃類」為第9個大分類；使用者決定不逐筆複查，直接移除全部19筆有問題資料，最終保留402筆乾淨資料，遺漏部分留待玩家回報機制（第20項）補足 |
| 第26次 | 小鎮總覽視覺概念+美術風格決策流程 | 新增「小鎮總覽」為登入後首頁，包裝既有頁面（圖書館=收藏總覽、家=收藏庫/想要清單），未來擴充建築（工廠/超市/市集/服飾店）以「看得到點不進去」方式先呈現，不超出MVP範圍；釐清資料夾/路由層級屬低成本可調整項目；定案美術風格分三階段處理（MVP先用平面圖示風格，不現在決定最終風格） |
| 第27次 | 「我的收納冊」功能討論（wireframe手繪圖） | 定位為新增獨立頁面，不取代`/browse`；曾考慮「小屋保險箱/書櫃」空間視覺化分享方式，因等同重做已排除的小屋訪問系統且易誘發比較心態而否決，改採沿用第16項分享連結機制的簡化版（每本收納冊可設公開/私人，公開產生唯讀連結，不做探索廣場頁）；格數採固定套用現實小卡冊規格（1/2/3/4/6/8/9格），不做自由拖拉排版；資料庫新增collection_albums/album_pages/album_slots三表；規劃完成，待整理成Claude Code開發指令 |
| 第28次 | 玩家互助補圖功能定案（v1） | 定案 v1 核心簡化規則：只在商品完全沒有公版代表照時開放玩家提交候選圖，已有公版圖後不開放取代，避免比較心態與審核負擔（呼應12-1）；審核比照第20項模式（Table Editor人工審核，不建UI）；站長自己上傳免審核；通過審核可獲10枚遊戲幣獎勵，但遊戲幣系統本身仍屬第10項已排除範疇，尚待建置；上傳需授權公開展示（呼應第23項） |

---

## ✅ 已完成的小任務（非規劃類）

- [x] 建立 GitHub repo + 上傳佔位首頁
- [x] 串接 Vercel 並成功部署，取得免費子網域網址
- [x] 商品分類系統定案（大分類/細分類 + 賞別/製造商/圖片連結欄位設計）

---

## 🖥️ Claude Code 開發進度（2026-07-28 更新）

> 這一節專門記錄「程式碼實際做到哪」，跟上面的「規劃討論紀錄」分開。每次請 Claude Code 做完新功能，回來更新這節就好，不用整份文件都重看。

### 已完成頁面/功能

| 路徑 | 說明 |
|---|---|
| `/`（首頁） | 專案簡介 + 精選商品 + 註冊/登入 CTA；文案於 2026-07-28 依使用者提供文字定稿（標題/副標、現在已可做的事、開發中預告、結尾號召） |
| `/login`、`/register`、`/forgot-password`、`/reset-password`、`/auth/callback` | Email+密碼登入註冊、忘記密碼、Supabase Auth 回呼 |
| `/browse` | 收藏庫瀏覽，含篩選面板（作品/系列/大小分類/賞別/角色）+ 分頁 |
| `/products/[id]` | 商品詳情頁：標記已擁有/虛擬收藏/想要、編輯備註+入手日期、上傳照片（前端自動壓縮長邊至1600px再傳 Supabase Storage，上傳前需勾選「實體拍攝＋授權使用」確認框才能選檔，2026-07-30 新增） |
| `/town` | 小鎮總覽（登入後首頁）：圖書館(=dashboard)、家(=browse/wishlist) 可進入，工廠/超市/市集/服飾店灰階佔位 |
| `/dashboard` | 我的收藏紀錄：全站(開放集合,原始數字)/依系列(封閉集合,%)/依角色(開放集合,原始數字) 三層統計，實體與虛擬分開顯示 |
| `/wishlist` | 想要清單，重用 browse 的 ProductCard，篩選 `owned_status='wanted'` |
| `/settings` | 暱稱、社群連結、改密碼、刪除帳號（需輸入確認字串，`/api/account/delete` 用 service role 清 Storage 照片後刪除 auth 使用者，DB 資料靠 FK cascade 自動清除） |
| Header 導覽列 | 全站共用，依登入狀態顯示不同連結 |
| `supabase/schema.sql` | 完整建表 SQL + RLS 政策 + `collection-photos` Storage bucket 設定，可重複執行 |
| `scripts/import-products.mjs` | CSV 批次匯入腳本（`npm run import:products`），必填欄位檢查+重複偵測(系列+名稱+賞別)+內容規則警告(綜合標籤/分類清單外/日文殘留/價格格式)，執行後印出成功/跳過/警告報告 |
| `/albums` | 我的收納冊清單：新增收納冊、刪除收納冊，卡片顯示頁數與公開/私人狀態 |
| `/albums/[id]` | 收納冊編輯：九選一版面模板新增頁面、上下頁切換、**常駐側邊商品清單**（點格子選定目標→點商品放入，不做拖拉，見已定案項目29-1）、格子右上角 × 直接移除、公開/私人切換（切公開自動產生 `share_token`）+ 複製分享連結 |
| Footer 版權免責聲明 | 全站共用（`components/Footer.tsx`），2026-07-28 依使用者提供文字定稿，小字灰階呈現 |
| `/albums/share/[token]` | 公開唯讀檢視，不需登入；只顯示商品名稱與使用者自己上傳的照片，不顯示備註/入手日期/擁有狀態；未公開或 token 不存在一律回 404，不透露私人收納冊存在 |

驗證狀態：`npm run build`、`npx tsc --noEmit`、`npm run lint` 均通過；dev server 逐一路由 curl 測試無誤（受保護頁面未登入時正確 307 導向 `/login`）；`products.csv` 已實際匯入資料庫，`/browse` 顯示 400 件商品共 9 頁，`/products/1` 等詳情頁確認能正確顯示真實資料。**正式環境（Vercel）已於 2026-07-28 部署成功並用 curl+瀏覽器截圖驗證首頁/browse/商品詳情頁皆正常顯示真實資料。**收藏標記/備註/照片上傳/我的收藏統計/想要清單/個人設定/刪除帳號整條流程也已用真實測試帳號在正式環境操作過一遍，全部正常（刪除帳號會連 Storage 照片、`auth.users`、`user_collections` 一併清乾淨，已用腳本查證）。

### CSV 匯入結果（2026-07-28）

- 成功匯入 400 筆，0 筆內容規則警告（代表先前人工清理過的資料品質不錯）。
- **2 筆因來源 CSV 本身欄位錯位被自動跳過，尚未匯入**（不是腳本的 bug，是 `products.csv` 這兩列資料本身在 character_aliases 之後少打了值，導致後面欄位全部錯位）：
  - 第41列「立體機動裝置 1/12 比例模型」（扭蛋 進擊的巨人～立體機動裝置～系列）
  - 第66列「立體機動裝置 水壺」（USJ 日本環球影城～進擊的巨人THE REAL～系列）
  - 修法：打開 `products.csv` 找到這兩列，把 manufacturer/official_price 欄位補上正確值（原始資料看起來分別是 BANDAI/¥500/2021-11-20 與 USJ/¥3200/2017-01-13），改好後重跑 `npm run import:products` 即可（腳本有重複偵測，其餘400筆不會被重複匯入）。

### 部署踩雷記錄（2026-07-28，供之後參考）

Vercel 專案 `qa-clinic-taiwan/collector-village` 是沿用舊靜態佔位頁時代建立的專案，**Build and Deployment 設定裡 Framework Preset 卡在「Other」**（沒有自動偵測成 Next.js）。第一次 push 新 Next.js 專案上去時，Vercel 用「Other」模式建置，結果整個網站每個路徑都回應 Vercel 平台層級的 `404 NOT_FOUND`（不是我們 App 的 404 頁，是 Vercel 直接擋在前面，連 Runtime Logs 都沒有任何紀錄，因為根本沒有呼叫到任何 serverless function）。

排查方式：去 Vercel Dashboard → 專案 → Settings → Build and Deployment，看到 Framework Preset 顯示「Other」而非「Next.js」。
修法：把 Framework Preset 改選為「Next.js」（Build/Output/Install/Dev Command 全部維持不 Override，用 Next.js 預設值即可）→ Save → 回 Deployments 列表對最新的部署按「Redeploy」（不勾 Use existing Build Cache）→ 重新建置後即恢復正常。

**如果之後又遇到網站回應 Vercel 的 `NOT_FOUND`（純文字、非 App 樣式）**，優先檢查這個 Framework Preset 設定，而不是懷疑程式碼或環境變數。

### 資料庫直連工具（2026-07-28 新增）

- `.env.local` 新增 `DATABASE_URL`（Supabase 專案 Settings > Database > Connection string，用 Session pooler 那組），加上 `scripts/run-sql.mjs`（`node --env-file=.env.local scripts/run-sql.mjs <sql檔案>`），Claude Code 之後可以直接執行 schema 變更，**不再需要每次請你手動貼到 Supabase SQL Editor**。
- 用途僅限本機開發時執行 SQL；正式站（Vercel）不需要、也沒有設定這個環境變數。

### 我的收納冊功能（2026-07-28，依 `claude-code-instructions-albums.md` 規格開發）

- 新增 `collection_albums`／`album_pages`／`album_slots` 三張表 + RLS 政策（本人可完全操作，`is_public=true` 時任何人含未登入訪客可讀）。
- **規格書裡 `collection_albums.user_id` 原寫 `INT`，但實際 `public.users.id` 是 `uuid`（Supabase Auth 設計），已修正為 `uuid` 才能正確建表**，供之後對照規格書時留意這個落差。
- 「同一商品不可重複選入同一收納冊」在資料庫層沒有辦法直接用唯一約束表達（`album_slots` 只知道 `page_id`，不知道所屬 `album_id`），改在應用層（`lib/supabase/albums.ts` 的 `assignSlotProduct`）跨頁查詢比對後擋下。
- 公開分享頁的照片簽名網址：訪客未登入、無法用一般 RLS 規則簽出他人私人 Storage 裡的照片，`/albums/share/[token]` 改用 service role（`createAdminClient`）產生簽名網址，前提是已經先用一般權限的 client 確認過 `is_public = true` 才會走到這步，範圍有限縮，不是整頁繞過權限檢查。
- 已用真實測試帳號在本機 dev server 走過一遍：新增收納冊→選 6 格版面→選 2 件商品入格→確認同商品從可選清單消失→移除格子→新增第 2 頁（4格）→翻頁確認資料正確→設公開產生分享連結→無 cookie 的 curl 確認公開頁不需登入可看、且只顯示商品名稱→改回私人後分享連結立即 404→刪除收納冊確認 `album_pages`/`album_slots` 隨 cascade 一併清除→刪除帳號確認收納冊也隨 cascade 清除。全部正常。
- **UI 於同日依使用者手繪 wireframe（`idea/收納冊初版.png`）調整為常駐側邊商品清單 + 點選式放入**（不做拖拉，理由與討論見已定案項目29-1），已重新用測試帳號驗證選入/移除皆正常。

### 首頁文案與 Footer 版權聲明（2026-07-28）

- 首頁（`app/page.tsx`）文案改版：標題/副標、簡介、「現在已經可以做的事」「接下來正在開發」兩欄、結尾號召，皆依使用者提供文字定稿；精選商品區塊與註冊/瀏覽 CTA 保留不變。
- Footer（全站共用）版權免責聲明文字更新為正式版本，小字灰階呈現，不搶眼但每頁可見。

### 已知缺口 / 下一步待辦（依優先順序）

1. ~~本機已 commit 但尚未 push~~ **已於 2026-07-28 push 並確認正式站部署成功**（收納冊功能、資料庫直連工具、首頁文案、Footer 聲明皆已上線，curl 驗證過首頁文案與 `/albums` 保護路由正常）。
2. 修正 `products.csv` 中 2 筆錯位資料並重新匯入（非急迫，屬於資料完整性小尾巴，見上方「CSV 匯入結果」）。
3. 商品清單頁 pagination 目前每頁 48 筆，資料量變大後可視需要調整。
4. 目前 `collector-village-git-main-qa-clinic-taiwan.vercel.app` 這個分支別名網址有 Vercel Deployment Protection（會導到 SSO 登入頁），主要正式網址 `collector-village-nine.vercel.app` 不受影響，如不需要可忽略。

---

## 🗂️ 待討論／尚未決定（2026-07-28 新增，來自跟 Claude Code 的聊天）

以下是討論過但還沒實作、之後要開始做這塊時記得回來完善的項目。

### 1. products.csv 資料審核流程
- 現有 400 筆資料可能有錯，Peggy 打算重新逐行檢查一次（尚未開始，Peggy 會自行找時間做）。
- 採用的方法：**不刪除任何東西，在原本內容下方（例如第500行後）新增一個區塊，審核確認沒問題的那一列就剪下貼到後面**。之後 Claude Code 只需要看「後面區塊」有哪些商品就知道是審核過、可以拿去同步資料庫的。
- **前置準備已完成（2026-07-30）**：`products.csv` 已有「商品編號」欄（1~402 依現有列順序流水編號）；`products` 表已加上 `product_code int unique` 欄位（`supabase/schema.sql` 第3節）；已執行 `scripts/backfill-product-codes.mjs`，把現有資料庫 400 筆商品依「系列+商品名稱+賞別」比對回填對應編號，**400 筆全部成功回填、0 筆遺漏**（另 2 筆是 CSV 裡本來就因欄位錯位尚未匯入的舊資料，見「已知缺口」第2項，之後修正重新匯入時會自動帶入編號）。`scripts/import-products.mjs` 也已同步更新，之後新匯入的商品會一併寫入 `product_code`。
- 之後 Peggy 開始逐行校對、修正錯字時，不論文字內容怎麼改，都可以用這個編號準確比對回資料庫同一筆資料做覆蓋更新，不受文字比對失準影響。**尚未實作的部分**：目前腳本還是只會「新增」，沒有「依 product_code 覆蓋更新既有資料列」的功能，等 Peggy 校對出第一批要更新的資料再請 Claude Code 加這段邏輯即可，不用現在预先做。

### 2. 圖片儲存規劃
- 大量商品圖片（光巨人可能上千張，之後還有其他 IP）**不要放進專案資料夾跟著 git 走**，會讓 repo 體積暴增、clone/push 變慢，且撞到 GitHub 檔案大小限制。
- 沿用專案已經在用的 Supabase Storage，不需要另外接 Google 雲端硬碟：
  - 審核階段本機用來對照資料的圖：本機資料夾存放即可，加進 `.gitignore`，不需要進 git 版控。
  - 真正要在網站上顯示的圖：上傳到 Supabase Storage，網站只存路徑/網址。

---

## 下一步建議

從「待完成項目」清單中挑一項繼續往下細化。個人建議優先順序：
1. 網站頁面結構規劃（讓你腦中有清楚畫面，之後跟 Claude Code 溝通更精準）
2. 《進擊的巨人》商品資料 CSV 整理（工作量最大，可以拆多次慢慢做，現在欄位設計已經定案，可以直接照欄位填）
3. 收藏進度計算邏輯規則